# 명도대비 검증 게이트 결과 (US-016 / DESIGN.md §9 P6-4)

측정일 2026-08-01 · 브랜치 `worktree-ui-refactor` · 기준 커밋 `1b40798`
근거 규격 DESIGN.md §7.3(라이트 점검표 8행) · §3.3(의미색·★★ 솔리드 검증) · §3.2(잉크 표면 규율)

> **이 문서는 판정이지 수정이 아니다.** US-016 AC가 "AA 미달이 남아 있으면 목록으로 남긴다 —
> **임의로 값을 바꾸지 말고 보고한다**"를 명시하고, §7.3 8행의 방법 칸도 전부 "색출/측정"이다.
> 그래서 이번 커밋에는 **색을 바꾼 `src/` 변경이 하나도 없다.** 처분은 후속 story 소관이다.

---

## 0. 판정 요약

| §7.3 점검 행 | 기준 | 판정 | 근거 |
|---|---|---|---|
| 1. 본문 링크 대비 | ≥4.5:1 (바탕 `#f9fafb`) | **통과**(산문 링크) / **미달**(사이트 크롬 링크) | §3 |
| 2. 링크 식별(색 외 단서) | 밑줄 렌더 | **통과** | §3 |
| 3. 솔리드 배경 + 잉크 | ≥4.5:1 | **미달 10종 / 29개** | §3 |
| 4. 틴트 배경 + 잉크 | ≥4.5:1 | **미달 6종 / 25개** | §3 |
| 5. 보조 텍스트(표면별) | ≥4.5:1 | **미달 55종 / 98개** | §3 |
| 6. 캡션·날짜 tertiary 미사용 | 콘텐츠 금지 | **미달 — 78개 호출부** | §5 |
| 7. 최소 글자 크기 | ≥11px | **통과**(최소 11.0px, 375/768/1280 전수) | §3 |
| 8. 터치 타깃 | ≥44×44px | **미달 41종** | §3 |

**게이트 결론: 통과 3 / 미달 5.** DESIGN.md:796의 문언("이 단계 없이 P2·P3을 완료로 선언하지 않는다")대로
**P2·P3은 아직 완료로 선언할 수 없다.** 다만 미달의 성격은 P2·P3이 만든 토큰이 틀렸다는 것이 아니라,
**토큰이 닿지 않은 호출부가 남았다**는 것이다 — 신설된 토큰(`--color-accent-ink`, 의미색 `-ink`/`-bg` 15종,
`--color-brand-*`)을 실제로 쓴 자리는 **전부 통과**했다(§3 통과 목록 참조).

라이트 미달 **96종 / 358개**, 다크 미달 **51종 / 281개** (시그니처 = `태그+클래스+전경색+배경색+글자크기` 조합).

---

## 1. 측정 방법

- playwright 임시 스펙(`tmp-us016-contrast.spec.ts`·`tmp-us016-hover.spec.ts`, **커밋 전 삭제**)으로
  18개 라우트 × 라이트/다크를 `1280×900`에서 순회하고, `body *` 중 **직접 텍스트 노드를 가진 보이는 요소 전수**를 쟀다.
- **배경은 조상 체인을 타고 올라가며 알파를 합성**한다. 텍스트 노드의 `background-color`는 대부분
  `rgba(0,0,0,0)`이라 그대로 읽으면 검정으로 오독된다. 불투명 배경을 만날 때까지 올라가 아래→위로 합성했다.
- 색 문자열은 **canvas 1픽셀 렌더로 정규화**한다. 이 레포의 다크 토큰은 `color-mix`/8자리 hex/`oklab()`으로
  나와 정규식 파싱이 깨진다(progress.txt Codebase Patterns의 알파 틴트 함정).
- 대형 텍스트 예외(3:1)는 **요소별 계산값**으로 판정한다 — `≥24px` 또는 `≥18.66px && weight≥700`.
  버튼 라벨(13~15px/500~600)은 예외에 해당하지 않는다(§3.3 ★★와 같은 기준).
- hover는 `page.hover()`로 실제 호버해서 rest와 따로 쟀다.

### 커버리지와 갭 (전수라고 주장할 수 있는 범위)

측정한 라우트 18개: `/` `/guide` `/tools` `/tools/contract-check` `/tools/holiday-pay` `/sanction`
`/database` `/blog` `/cases` `/news` `/faq` `/search` `/contact` `/privacy` `/checklist` `/harassment`
`/subsidy` `/lecture/ai-isan-2026/textbook`.

**측정하지 못한 것(= 통과한 것이 아니다):**

| 갭 | 이유 |
|---|---|
| DB 기반 목록·상세(`/blog/[slug]`·`/cases/[id]`·`/interpretations/[id]`·`/database` 카드·`/news` 목록) | `.env.local`이 **placeholder Supabase**라 이 환경에서 0건이 렌더된다(워크트리·본 레포 동일). 빈 상태만 측정됨 |
| `/admin`·`/admin/clusters` | 토큰 게이트. tertiary 호출부 **14개**가 이 파일에 몰려 있다(§5) |
| `/tools/contract-check` 판정 배지 4색(violation·risk·ok·needs_data) | 분석 완료 후에만 렌더. **정적 hex 상수**(`ContractCheckClient.tsx:29-34`)라 DESIGN.md:176의 산술치(5.30/4.51/4.57/6.15) 를 그대로 인정하고 실렌더는 미확인 |
| 상호작용 상태 전반(포커스 링, 선택 칩 활성, 에러 박스) | rest 상태만 순회. hover는 주 CTA·FAB·본문 링크 3개만 실측 |
| `/manual`·`/stats`·`/pkb`·`/terms`·`/ai`·`/faq/[category]` 등 5개 라우트 | 시간 예산 |

---

## 2. §3.3 ★★ "솔리드 배경 + 흰 잉크" 4개 미달 항목이 실제 화면에서 어떻게 처리됐나 (AC2)

DESIGN.md §3.3 ★★ 표가 지목한 4건을 **실렌더에서 하나씩 찾아** 확인했다.

| §3.3이 지목한 값 | 문서가 지시한 처분 | 실제 화면 | 판정 |
|---|---|---|---|
| `#3182f6` (accent, 라이트) **3.71** | 주 CTA 배경을 `--color-accent-ink` `#1b64da`로 | **주 CTA는 이행됨** — `.nav-cta` 실측 `#ffffff` on `#1b64da` = **5.41:1**, hover `#1957c2` = **6.60:1**. 홈·`/checklist`·`/subsidy`의 주 버튼도 5.41 | ✅ 이행 |
| 〃 | 〃 | **FAB은 미이행** — `FloatingChatButton.tsx:18`이 아직 `--color-accent` + 흰 잉크. 실측 **3.71:1**, 전 라우트 18곳. §6.7이 `--color-accent-ink`(5.41)를 지정했는데 반영 안 됨(US-013이 넘긴 항목) | ❌ 미달 |
| 〃 | 〃 | **인라인 버튼 8종 미이행** — `/database` 탭, `/blog` 필터 칩, `/faq` 카테고리, `/search` 검색 버튼·CTA, `/contact` 문의하기, `/harassment` 필터가 흰 잉크 on `#3182f6` = **3.71:1** | ❌ 미달 |
| `#059669` (success, 라이트) **3.77** | **성공은 솔리드 배경으로 쓰지 않는다**(필요하면 `#047857` 5.48) | **정면 위반이 살아 있다** — `guide/page.tsx:97` O/X 배지가 `backgroundColor: var(--color-success)` + `color: var(--color-bg-surface)`(라이트=흰색). 실측 **3.77:1**(문서 예측치와 정확히 일치). 짝인 X 배지 `#ffffff` on `#dc2626` = **4.83:1**은 통과 | ❌ 미달 |
| `#4593fc` (accent, 다크) **3.07** | 다크 주 CTA는 배경 `#4593fc` + 잉크 `#0f1117`(6.14) | **이행됨** — 다크 `.nav-cta` 실측 `#0f1117` on `#4593fc` = **6.14:1**. ★ 단, **`--color-accent` 자체는 `.dark`에서 재정의되지 않았다**(실측 다크에서도 `#3182f6`). §3.3 표의 "다크 `#4593fc`" 는 `--color-accent-ink`에만 적용됐다 → 문서가 예측한 3.07 케이스는 **존재하지 않고**, 대신 다크에서도 흰 잉크 on `#3182f6` = 3.71이 남는다 | ✅ 이행(단 문서 정정 필요) |
| `#64a8ff` (accent-hover, 다크) **2.45** | hover에도 어두운 잉크 유지 | **이행됨** — 다크 `.nav-cta:hover` 실측 `#0f1117` on `#64a8ff` = **7.72:1** | ✅ 이행 |

**요약:** 4건 중 **주 CTA 계열 3건은 이행**, **FAB·인라인 버튼·`/guide` 성공 배지는 미이행.**
문서 정정 1건: §3.3 표의 다크 `--color-accent` `#4593fc`는 코드에 반영되지 않았다(`globals.css` `.dark`에 `--color-accent` 선언 없음).

---

## 3. §7.3 라이트 점검표 8행 상세

### 행 1 — 본문 링크 대비 (≥4.5:1)

| 대상 | 라이트 | 다크 | 판정 |
|---|---|---|---|
| `.blog-content .blog-link` (산문) | `#1b64da` on `#f9fafb` = **5.18** / hover `#1957c2` = **6.31** | `#4593fc` = **6.14** / hover `#64a8ff` = **7.72** | ✅ (US-014가 이행) |
| `.chat-markdown a` | **5.18** | **5.39** | ✅ |
| `contact:210` 단독 URL 링크 | `#1b64da` on `#ffffff` = **5.41** | 5.39 | ✅ (US-014 보완) |
| **푸터 링크**(`layout.tsx` 4개, 전 라우트) | `#3182f6` on `#f9fafb` = **3.55** | 5.08 | ❌ DESIGN.md:197이 적은 그 숫자 |
| `.nav-link` 활성 | `#3182f6` on `#e8f3ff` = **3.31** | 4.45 | ❌ |
| 카드 내 액션 링크(`유사 판례 검색`·`핵심판례 6선 전체 보기`·`자세히 보기`) | `#3182f6` on `#ffffff` = **3.71** | 4.46 | ❌ |
| `.toc-link`(`/guide` 목차) | `#b0b8c1` on `#f9fafb` = **1.92** | 4.09 | ❌ 최악 |

→ **산문 링크는 전부 통과, 사이트 크롬 링크가 전부 미달.**
US-014가 "나머지 22건은 §6.8 밑줄 예외지만 색 규칙까지 면제되는 건 아니다 → US-016 입력"으로 넘긴 그 항목이며,
측정 결과 **넘긴 판단이 옳았다**(3.31~3.71로 전부 AA 미달).

### 행 2 — 링크 식별 (색 외 단서)

- 산문 링크 3경로(`.blog-content`·`.chat-markdown`·`MarkdownSnippet`) + `contact:210`: **상시 `underline`** 렌더 확인
  (`text-decoration-line: underline`, offset `0.2em`, thickness `1px`).
- 밑줄 없는 `<a>` 25종은 전부 §6.8 예외(네비·칩·카드 전체 링크·CTA·푸터 네비)에 해당 — US-014의 14건 분류표와 일치.
- **✅ 통과.** 단 위 행 1이 보여주듯 "식별은 되지만 대비는 미달"인 자리가 다수다 — 두 기준은 별개다.

### 행 3 — 솔리드 배경 + 잉크 (≥4.5:1)

**미달 10종 / 29개** (라이트):

| 대비 | 전경 on 배경 | 자리 |
|---|---|---|
| **1.63** | `#b0b8c1` on `#e5e8eb` | 미완료 스텝 원 — `ContractCheckClient.tsx:61`·`HolidayPayCalculator.tsx:39` (다크 2.82) |
| **3.31** | `#3182f6` on `#e8f3ff` | 활성 `.nav-link`(11 라우트), `/cases` 카테고리 칩 2종 |
| **3.71** | `#ffffff` on `#3182f6` | FAB(18 라우트) + `/database`·`/blog`·`/faq`·`/search`·`/contact`·`/harassment` 버튼 8종 |
| **3.77** | `#ffffff` on `#059669` | `/guide` O 배지 (§3.3 ★★ 명시 금지) |
| **4.19** | `#6b7684` on `#f2f4f6` | `/faq` 건수 칩, `/checklist` `참고` 배지 |
| **4.42** | `#6b7684` on `#f9fafb` | `/cases`·`/database` 상단 라벨 칩 |

**통과한 솔리드**(신설 토큰이 닿은 자리): `.nav-cta` 및 주 CTA 6종 `#ffffff` on `#1b64da` **5.41** ·
X 배지 `#ffffff` on `#dc2626` **4.83** · 브랜드 노랑 `#191f28` on `#facc15` **10.81** / on `#fde047` **12.56** ·
`/tools` `검증` 배지 `#4e5968` on `#e5e8eb` **5.78** · `/guide` 스텝 원 `#15803d` on `#dcfce7` **4.57**.

### 행 4 — 틴트 배경 + 잉크 (≥4.5:1)

라이트는 §6.4/§6.6 규격을 따른 배지·배너가 **전부 통과**한다:
`#92400e` on `#fef3c7` **6.37** · `#991b1b` on `#fef2f2` **7.60** · `#1957c2` on `#e8f3ff` **5.87** ·
`#854d0e` on `#fef9c3` **6.38** · `#1b64da` on `#e8f3ff` **4.82**.

**미달은 전부 다크에서 나온다(6종 / 25개)** — 원인이 하나다:

> ★★ **`.dark`가 `--blue-50`만 알파 틴트로 뒤집고 `--blue-200`/`--blue-600`/`--blue-700`은 뒤집지 않는다**
> (`globals.css:200`에 `--blue-50` 재정의만 있다). `backgroundColor: var(--blue-50)` + `color: var(--blue-600|700)`
> 쌍을 직접 쓴 호출부는 다크에서 **배경만 어두워지고 잉크는 라이트 파랑 그대로** 남는다.

| 대비(다크) | 자리 |
|---|---|
| **2.03** | `HolidayPayCalculator.tsx:450` 안내 박스 (`--blue-700` 폴백 hex 동반) |
| **2.16** | `contact/page.tsx:136` `접수 폼` 칩 (라이트 5.87 → 다크 2.16) |
| **2.51** | `BetaBanner.tsx:38-40` 전 라우트 배너 4요소 · `ArticleCard.tsx:13` · `CaseCard.tsx:20` · `contact:219`·`subsidy:117` H2 |
| **3.06** | `HomeClient.tsx:135` 시행일 칩 · `ComparisonTable.tsx:17` · `contact`/`subsidy` 목록 |
| **3.83** | `/cases` 카테고리 칩 2종 |

영향 파일(직접 램프 쌍 사용): `BetaBanner` `ArticleCard` `CaseCard` `StepDiagram` `ComparisonTable`
`HomeClient` `contact` `subsidy` `cases/[id]` `faq` `database/_components/*` `ChatInterface` `interpretations/[id]`.
**처분은 값 변경이 아니라 호출부를 `--color-info-*` / `--color-accent-*` 역할 토큰으로 옮기는 일이다**(§3.3이 이미 지정한 매핑).

### 행 5 — 보조 텍스트 (표면별 ≥4.5:1)

**미달 55종 / 98개.** 두 갈래다.

**(a) `--grey-500` `#8b95a1` — 문서에 규율이 없는 잉크 (19종 / 40개)**
`§3.2` 표는 primary/secondary/tertiary 3단만 규정하는데, 실제 화면의 소개문·부제·설명문 상당수가
`style={{ color: 'var(--grey-500)' }}`로 직접 램프를 참조한다. 실측 **바탕 `#f9fafb` 위 2.91:1 / 흰 카드 위 3.04:1**
— tertiary 다음으로 나쁘다. `/guide` `/cases` `/contact` `/checklist` `/tools` `/manual` `/ai` `/faq` 등 광범위.
(다크에서는 `--grey-500`이 재정의되지 않아 어두운 바탕 위에서 오히려 통과한다 — **라이트 전용 결함**이다.)

**(b) `--color-text-secondary` `#6b7684` — §3.2 규율 2 위반 (36종 / 58개)**
"secondary는 흰 표면 위에서만" 규율대로면 페이지 바탕 위에는 `--grey-700`을 써야 한다.
페이지 바탕(`#f9fafb`) 위 secondary = **4.42:1**로 47개 인스턴스가 미달. 틴트 표면 위는 더 낮다
(`#eef4fb` 4.18 · `#f2f4f6` 4.19 · `#f7f8f9` 4.35 · `#fff8e6` 4.36).
US-004가 "`/tools` 면책 문구 4.42 — US-016 점검표에 올릴 것"으로 넘긴 항목이 이 집합에 포함된다.
★ 같은 story가 기록한 **회귀**(`/tools` 카드 설명문 `#475569` 7.58 → `#6b7684` 4.62)도 확인했다 —
흰 카드 위라 4.62로 AA는 통과하지만 수치는 실제로 내려갔다.

### 행 6 — 캡션·날짜 tertiary 미사용 → §5로 분리

### 행 7 — 최소 글자 크기 (≥11px) ✅ 통과

375 / 768 / 1280 세 폭에서 6개 라우트를 재측정: **11px 미만 요소 0개, 최소값 정확히 11.0px**.
`--text-xs`가 `clamp(0.6875rem, …, 0.75rem)` = 하한 11px이라 375px에서도 11px에서 멈춘다.
`text-[10px]` 같은 임의값도 grep 0건. **이 행만 유일하게 여유 없이 딱 통과**한다 —
`--text-xs`의 하한을 내리는 변경은 즉시 이 행을 깨뜨린다.

### 행 8 — 터치 타깃 (≥44×44px)

**41종 미달**(1280 기준, `<input>` 제외). 좁은 폭에서도 개수는 거의 같다(375px에서 라우트당 9~15종).

| 크기 | 자리 |
|---|---|
| 76×**14** / 79×**14** | 푸터 링크 4개 × 18 라우트 (가장 심함) |
| 23×**20** | `BetaBanner` 닫기 `✕` |
| 68×**24** | `BetaBanner` `알림 받기` |
| 36×**26** / 103×**26** | 헤더 `.nav-link` 전량 |
| 34×**34** | 헤더 테마 토글 버튼 |
| 그 외 | 칩·필터 버튼 다수(28~38px), `select` 2종(36·42px) |

★ 반대로 **`min-h-[44px]`를 명시한 주 CTA 계열은 전부 통과**한다 — 규격을 붙인 곳은 지켜지고 있고,
안 붙은 크롬 요소가 남은 형태다.

---

## 4. AA 미달 잔여 목록 (AC3) — 원인별 집계

**임의로 값을 바꾸지 않았다.** 아래가 그 "남아 있는 목록"이다.

### 라이트 (정본) — 96종 / 358개

| 종 | 개 | 최저 | 원인 잉크 | 대표 자리 |
|---|---|---|---|---|
| 16 | 123 | 3.31 | `--color-accent` `#3182f6` 전경 | 푸터·네비·카드 액션 링크, 칩 |
| 14 | 106 | 1.63 | `--color-text-tertiary` `#b0b8c1` | 푸터 2줄, `.toc-link`, 판례 메타, 빈 상태 |
| 36 | 58 | 4.18 | `--color-text-secondary` `#6b7684` (표면 규율 위반) | 페이지 바탕·틴트 위 설명문 |
| 19 | 40 | 2.71 | `--grey-500` `#8b95a1` | 소개문·부제 |
| 10 | 29 | 3.71 | 흰 잉크 on 솔리드 | FAB, 인라인 버튼 8종, `/guide` O 배지 |
| 1 | 2 | 4.47 | `text-yellow-700` `#a65f00` | `/tools/holiday-pay` 관련 글 링크 |

### 다크 (보조) — 51종 / 281개

| 종 | 개 | 최저 | 원인 |
|---|---|---|---|
| 14 | 106 | 2.82 | `#6b7684`(다크 tertiary/라이트 secondary 값)가 어두운 표면 위 |
| 10 | 82 | 2.03 | `--blue-700` 잉크 미반전 (§3 행 4) |
| 10 | 46 | 3.83 | `--color-accent` 미반전 |
| 6 | 23 | 3.71 | 흰 잉크 on `#3182f6` |
| 3 | 11 | 3.06 | `--blue-600` 잉크 미반전 |
| 3 | 7 | **1.29** | `privacy/page.tsx:14` `text-gray-800` — **Tailwind 팔레트 유틸이라 다크에서 반전 안 됨** |
| 1 | 1 | **1.04** | `SubscribeForm.tsx:89` `var(--color-accent-soft, #fff8e6)` — **죽은 토큰의 폴백**이 카드를 라이트 고정 크림색으로 칠하고, 그 위에 다크 잉크 `#f2f4f6`가 얹힌다 |
| 1 | 1 | 2.50 | `privacy/page.tsx:12` `text-gray-600` |
| 2 | 2 | 1.89 | 위 크림 카드 위 secondary |

★ **다크 최악 2건(1.04·1.29)은 스크린샷으로 육안 확인했다** — `/` 뉴스레터 카드의 제목이 배경에 거의 완전히 묻히고,
`/privacy` 본문 전체가 읽히지 않는다. 두 건 다 **이번 리팩터가 만든 것이 아니라 토큰화가 닿지 않은 잔여**다:
`SubscribeForm.tsx:89`는 US-012가 "죽은 토큰 `--color-accent-soft`, 처분은 US-016 소관"으로 넘긴 그 줄이고,
`/privacy`는 §7.2-5가 지목한 "Tailwind 팔레트 유틸 203회"의 표본이다.

---

## 5. `--color-text-tertiary`가 콘텐츠 텍스트에 쓰이지 않는다 (AC4) — **위반**

`grep -rn "text-tertiary" src/ --include=*.tsx` = **92건**. 분류:

| 분류 | 건수 | 판정 |
|---|---|---|
| 아이콘 색(`size={…}` 동반 lucide) | 10 | 허용 (§3.2 "비활성 아이콘") |
| 비활성 표면과 한 쌍(`backgroundColor`/`borderColor` 동반) | 4 | 회색지대 — 그중 스텝 원 2건은 **숫자 텍스트**라 실측 1.63:1 미달 |
| **콘텐츠 텍스트** | **78** | **위반** |

파일별 텍스트 호출부: `AdminClient` 14 · `cases/[id]` 9 · `DatabaseClient` 8 · `BlogClient` 7 ·
`interpretations/[id]` 7 · `CaseCard` 5 · `blog/[slug]` 4 · `NlrcCard` 4 · `AdminCard` 3 · `NewsClient` 3 ·
`ContractCheckClient` 3 · `database/page` 2 · **`layout.tsx` 2(푸터, 전 라우트 노출)** · 그 외 8파일 1건씩.
여기에 CSS 1건(`globals.css:491` `.toc-link`)이 더 있다.

**실렌더로 확인된 위반 14종**(DB 미연결이라 카드류는 빠졌다 = 실제는 이보다 많다):

| 대비 | 자리 | 내용 |
|---|---|---|
| 1.63 | `ContractCheckClient:61`·`HolidayPayCalculator:39` | 미완료 스텝 번호 |
| 1.92 | `layout.tsx:170,173` | 푸터 저작권·주소 (전 라우트) |
| 1.92 | `globals.css:491` `.toc-link` | `/guide` 목차 9개 |
| 1.92 | `guide` 사이드바 `목차` 헤더 | |
| 1.92 | `/blog`·`/news`·`/faq` 빈 상태 문구 | "등록된 글이 없습니다" 등 |
| 2.01 | `/cases`·`/guide` 판례 메타 33개 | 법원명·사건번호 |
| 2.01 | `/checklist` 항목 설명 13개 | 읽으라고 놓은 본문 |
| 2.01 | `/database` 안내문·라벨 | |

§3.2 규율 1이 "캡션·날짜·출처처럼 **읽으라고 놓은 글자**는 콘텐츠다. 특히 법령 정보 사이트에서
갱신일·시행일은 신뢰성의 핵심"이라고 못박은 바로 그 자리들이다.

### 기계적 리맵 표 (후속 story용 — 이번 커밋에서는 적용하지 않았다)

§3.2 규율이 이미 대상 값을 정해 두었으므로 새 값을 지어낼 필요가 없다:

| 지금 | 놓인 표면 | 옮길 곳 | 결과 대비 |
|---|---|---|---|
| `--color-text-tertiary` (텍스트) | 흰 카드 `#ffffff` | `--color-text-secondary` | 4.62 |
| `--color-text-tertiary` (텍스트) | 페이지 바탕 `#f9fafb` · 틴트 · `--text-xs` | `--grey-700` `#4e5968` | 6.81 |
| `--color-text-secondary` | 페이지 바탕 `#f9fafb` · 틴트 | `--grey-700` | 6.81 |
| `--grey-500` (텍스트) | 어느 표면이든 | `--color-text-secondary`(흰 카드) / `--grey-700`(그 외) | 4.62 / 6.81 |
| `--color-text-tertiary` (아이콘·플레이스홀더) | — | **그대로 둔다** | 규율상 허용 |

규모: tertiary 78 + secondary 표면위반 58 + grey-500 40 ≈ **호출부 170여 곳**. 별도 story가 필요하다.

---

## 6. 이전 이터레이션이 "US-016 입력"으로 넘긴 항목의 처분

### 대비 항목 (이 게이트가 판정함)

| 넘긴 story | 항목 | 판정 |
|---|---|---|
| US-013 | FAB이 `--color-accent`+흰 잉크 3.71 | **확인 — 미달**(§2). 18 라우트 |
| US-004 | `/tools` 면책 문구 4.42 | **확인 — 미달**(§3 행 5b) |
| US-004 | `/tools` 카드 설명문 7.58 → 4.62 회귀 | **확인 — AA는 통과, 수치는 후퇴**(§3 행 5b) |
| US-014 | 푸터 등 사이트 크롬 accent 링크 3.55 | **확인 — 미달**(§3 행 1). 크롬 링크 전부 3.31~3.71 |
| US-012 | 죽은 토큰 `--color-accent-soft` 폴백(`SubscribeForm:89`) | **확인 — 다크 1.04:1**(§4). 대비 결함으로 확정 |
| US-004 | 포커스 링 1.27:1 (WCAG 1.4.11 비텍스트 3:1) + `focus:outline-none`이 전역 `:focus-visible`을 특정도로 이김 | **미측정.** rest 상태만 순회했다 — 포커스 상태 전수는 별도 패스가 필요하다. **미해결로 남긴다** |

### 대비가 아닌 항목 (여기서 판정하지 않는다 — 다른 소관)

| 넘긴 story | 항목 | 왜 여기서 안 다루나 |
|---|---|---|
| US-013 | `.feature-card` 그림자 소유권(인라인 style vs 클래스) | 그림자 규격 문제. §5.4 소관 |
| US-014 | `@tailwindcss/typography` 미설치로 교재 `prose*` 전량 무효 | 페이지 단위 결함. 별도 story |
| US-014 | §6.6 반경 문서 10px vs 실장 14px | 문서 정정 |
| US-015 | `theme-color`가 OS `prefers-color-scheme`만 타서 사이트 토글과 어긋남 | 테마 축 문제 |
| US-009 | 배지 배경 밝기 vs 부모 대비(`vsParent`) | 이번 프로브는 **잉크 대비**만 잰다 |

---

## 7. 문서 정정 제안 (DESIGN.md)

1. **§3.3 표의 다크 `--color-accent` `#4593fc`는 구현되지 않았다.** `.dark`에 `--color-accent` 선언이 없어
   실측 다크에서도 `#3182f6`이다. 표를 고치거나 토큰을 추가하거나 — 어느 쪽이든 지금은 문서와 코드가 어긋나 있다.
2. **§3.2 표에 `--grey-500` 항이 없다.** 실제로는 소개문·부제에 40개 인스턴스가 쓰이고 라이트 2.91:1이다.
   3단(primary/secondary/tertiary) 모델이 화면을 다 설명하지 못한다.
3. **§7.3 표에 "램프 토큰 직접 참조 금지" 행이 없다.** 다크 미달 281개 중 93개가 `--blue-600/700` 직접 참조 하나에서 나온다.

---

## 8. 부속 자료

- 원시 측정값 JSON(`us016-{light,dark}.json`, `us016-small-*.json`, `us016-hover-*.json`)과
  증거 스크린샷 7장은 `.tmp-us016/`에 생성되며 **커밋하지 않는다**(재현 절차는 §1).
  ★ `test-results/`에 두면 병렬 인스턴스의 playwright 런이 통째로 비운다 — 실제로 첫 런의 산출물이 그렇게 소실됐다.
- 재현: `tests/e2e/tmp-us016-contrast.spec.ts` 를 다시 만들어 `npx playwright test tmp-us016 --workers=1`.
