/**
 * 목록 한 페이지에 몇 편을 보일지.
 *
 * **클라이언트 컴포넌트에 두면 안 된다.** `'use client'` 모듈에서 상수를 꺼내 쓰면
 * 서버 쪽에서는 클라이언트 참조 프록시로 바뀌어 값이 undefined 가 된다.
 * 2026-09-12 에 BlogClient 에 두었다가 `range(0, NaN)` 이 나가 목록이 통째로 비었다.
 * count 는 964 로 멀쩡히 오는데 행만 0건이라 "검색 결과가 없습니다"만 떴다 — 찾기 어려운 종류다.
 *
 * 12편이던 것을 24편으로 늘렸다. 960편을 12편씩 보이면 80페이지가 되고
 * 이전/다음 버튼밖에 없어 "글이 몇 개 없다"로 읽힌다 (2026-09-11 지적).
 */
export const PAGE_SIZE = 24;
