# yellowenvelope.kr 디자인 정본

> 이 문서가 이 레포의 디자인 정본이다. 기존 `DESIGN_GUIDE.md`(492줄)는 **대체됨**. 두 문서를 병합하지 마라.
> 최종 확인: 2026-08-01 / 대상: Next.js 16.1.6 + React 19 + Tailwind v4 + shadcn(new-york)

---

## 0. 이 문서를 읽는 법

### 0.1 우선순위

우선순위는 **질문의 종류에 따라 갈린다.** 하나의 서열로 쓰면 §9 마이그레이션이 §0.1에 막혀 집행 불가능해진다.

| 질문 | 정본 | 비고 |
|---|---|---|
| **"지금 값이 무엇인가"** (현행 기술) | `src/app/globals.css` → 실제 화면 코드 | 이 문서의 현황 서술이 코드와 다르면 **코드가 맞고 이 문서가 틀렸다.** 문서를 고쳐라 |
| **"값이 무엇이어야 하는가"** (규범) | 이 문서 (DESIGN.md) | 규칙·판단·목표값의 정본. 코드가 이와 다르면 **코드를 고쳐라**(§9) |
| — | `DESIGN_GUIDE.md` | **참조 금지.** 아래 어긋남 목록 참고 |

즉 §3.1 원시 램프 "변경 금지"는 *현행 값을 임의로 바꾸지 말라*는 뜻이고, §9가 지시하는 globals.css 수정은 *이 문서가 규범으로 정한 변경*이다. 전자가 후자를 막지 않는다.

### 0.2 `DESIGN_GUIDE.md`가 현행과 어긋나는 지점 (병합 금지 근거)

| 가이드 기술 | 실제 |
|---|---|
| `--color-danger` / `--color-success` / `--color-warning` (가이드 114-116행) | globals.css에 **없음**. 각 페이지가 생 hex로 하드코딩 |
| `--ease-standard` (가이드 364행) | 없음. 실재는 `--ease-expo-out` |
| `--text-hero: clamp(2.5rem, …)` | 실제 `clamp(1.75rem, 1rem + 4vw, 5rem)` (globals.css:91) |
| §5 콘텐츠 블록 5종(ArticleBlock/DefinitionBlock/CaseBlock/NoteBlock/CriteriaBlock) | 구현체 없음 |
| §9 `detectBlockType` 자동 감지 | 구현체 없음 |
| §4 3패널 문서 레이아웃 | `.docs-layout` CSS만 존재, 실사용 1곳 |

### 0.3 표기 규칙

- `(신규)` = 이 문서에서 **처음 정한 값**. 조사 시점 코드/문서 어디에도 없었다.
- 마커가 없는 수치는 전부 `globals.css` 또는 실제 화면 코드에 실재하는 값이다.
- 전체 신규값은 **부록 A**에 일람으로 모아 두었다.
- 개수·회수를 적을 때는 **세는 방법을 함께 적는다**(예: "`src/**/*.tsx` 6자리 hex 284개"). 방법 없는 집계는 재현이 안 되고, 재현이 안 되면 근거가 아니다.
- 명도대비는 **WCAG 2.1 상대휘도 공식**으로 계산한 값이며, 배경을 함께 밝힌다(예: `#3182f6` on `#ffffff` = 3.71:1). 알파 틴트는 합성 후 배경이 표면마다 달라지므로 검증 표면을 명시한다.

**실측 기준 시점: 2026-08-01, 커밋 미고정.** 아래 집계는 그 시점 워킹트리 기준이며, 코드가 바뀌면 수치도 바뀐다.

---

## 1. 이 사이트가 무엇처럼 보여야 하는가

yellowenvelope.kr은 로그인 없이 검색으로 도착한 사람이 자기 상황에 해당하는 법 조항 하나를 찾아 읽고 나가는 곳이다. 그러므로 화면의 기본 상태는 **읽을 수 있는 문서**여야 하고, 인터페이스는 그 문서를 방해하지 않는 만큼만 존재한다. 흰 표면 위에 1px 경계선으로 문단을 나누고, 파랑은 "여기를 누르면 다음 문서로 간다"는 신호로만 쓰며, 노랑은 이 사이트가 노란봉투법을 다루는 곳이라는 표시를 도구 화면에서만 남긴다. 방문자는 대개 임금체불·해고·괴롭힘 같은 상태에서 오므로 화면은 다급하지 않게 굴어야 한다 — 경고색으로 겁을 주거나, 애니메이션으로 시선을 끌거나, 확정적으로 말하지 않는다. 시각적 성취의 기준은 "예쁘다"가 아니라 **긴 한국어 본문을 3분간 읽어도 눈이 안 아픈가**, 그리고 **이 정보가 법적으로 신뢰할 만해 보이는가** 두 가지다.

> ★ **현행과의 어긋남 — 오로라 그라디언트.** 위 문단은 페이지 바탕을 평면 `#f9fafb`로 전제하지만, `globals.css:240-249`의 라이트 `body`에는 blue/indigo/emerald 3중 `radial-gradient` + `background-attachment: fixed`가 실재한다(`.dark body`(183-188)는 더 진한 세트). 이것은 로그인 SaaS 랜딩의 어휘이지 공적 정보 문서의 바탕이 아니고, `background-attachment: fixed`는 모바일 스크롤 리페인트 비용을 만든다 — 이 사이트 트래픽은 대부분 모바일 검색 유입이다. **처분: 라이트 바탕은 평면 `var(--color-bg-primary)`로 되돌린다(§9 P4-4).** 다크는 알파가 0.08 이하라 눈에 띄지 않으므로 존치 여부를 별건으로 남긴다.

두 번째 기준("법적으로 신뢰할 만해 보이는가")을 담당하는 컴포넌트는 **§6에 아직 하나도 없다.** 갱신일·근거 조문·감수자·출처 표기의 규격은 §10에 미결로 올려 둔다 — 성공 기준을 걸어 놓고 수단을 안 준 상태임을 문서가 인정한다.

---

## 2. 다른 시스템에서 무엇을 가져오고 무엇을 버리는가

> **출처 표기.** 아래 「이식/기각」 절의 비교 대상은 사내 SaaS 두 개다. 이 문서는 공개 레포에 있으므로 **제품명·고유 토큰값·코드네임은 싣지 않는다** — 판단 근거만 남긴다.

이 레포는 사내 SaaS 두 개(이하 **사내 A**: 로그인형 관리 도구 / **사내 B**: 현장 작업 도구)와 디자인 어휘를 공유할 이유가 있는지 검토했다. 결론은 **원칙은 가져오고 어휘는 버린다**이다. 근거는 하나 — 그쪽은 로그인 SaaS(반복 사용자, 데이터 밀도, 브랜딩 통제)이고 여기는 공개 정보 사이트(1회성 SEO 방문자, 긴 산문, 공적 주제)다.

### 2.1 가져오는 것

| 항목 | 출처 | 가져오는 이유 |
|---|---|---|
| 웨이트 래더 400/500/600/700 고정, **300 이하 금지** | 사내 A | 제품 무관 — 한글 자형이 300에서 무너진다 |
| 한글 음수 트래킹 상한 **-0.01 ~ -0.02em** | 사내 A | 한글 자소가 붙기 시작하는 지점. 이 레포는 `.content-body`(-0.01em)에서만 준수 — 전역은 미적용(§4.3) |
| 본문 line-height **1.6 이상**(한글 보정), `word-break: keep-all` | 사내 A | 이 레포는 `.content-body`(1.65)·`.blog-content`(1.75)에서 국소 도달. `body`에는 없다(§4.3) |
| mono는 **라틴·숫자 전용**, 한글 렌더 금지 | 사내 A | 한글 글리프 부재 → faux-bold |
| 그림자는 위계가 아니라 "떠 있는 것"에만 | 사내 A | 이 레포 그림자 난맥(FAB 하드코딩, /tools 토큰 0건)을 정리할 유일한 판단 기준 |
| 헤어라인 = "잉크 선"이 아니라 **한 단계 낮은 서피스 톤** | 사내 A | 단, 사내 A의 **웜 그레이는 버린다**(§2.2). 이 레포는 Toss 쿨 그레이 램프 |
| **Charter 형식**(허용/금지를 배치 위치로 규정) | 사내 A | §3.4 노랑 규칙의 골격으로 그대로 이식 |
| 마이크로카피 규율: 단정 금지, 구현 어휘 노출 금지, 상태는 운영 언어로 | 사내 B | 법률 정보 사이트에 **더 강하게** 적용된다(§8) |
| 24px 초과 타이포는 전용 CSS 클래스로 (Tailwind 유틸 폴백 함정) | 사내 A | 이 레포도 Tailwind v4 + 프리셋 조합이라 동일 위험 |

### 2.2 버리는 것 — 근거 포함

| 버리는 것 | 출처 | 버리는 근거 |
|---|---|---|
| 금액 열 전용 어휘(전용 강조선·전용 네이비·전역 `tabular-nums`) | 사내 A | 이 사이트에 **정렬해야 할 금액 열이 거의 없다.** 확정/미확정 상태 구분도 없다. → tnum은 **정렬되는 표의 숫자 열 + `HolidayPayCalculator.tsx` 계산 결과에만** 국한 채택(§6.5) |
| 파스텔 블록 + 큰 반경 + pill CTA(내러티브 서피스) | 사내 A | 온보딩·대시보드 히어로의 어휘다. 20개 FAQ 카드와 긴 가이드 문서에는 "내러티브 서피스"라 부를 화면이 없다 |
| **다크-우선 기조** | 사내 A | 비로그인 SEO 방문자는 라이트로 도착한다. 라이트가 정본, 다크는 정직한 보조(§7) |
| 테넌트별 화이트라벨 색 슬롯 | 사내 A | 테넌트가 없다. 색은 전부 코어 소유 |
| 웜 그레이 팔레트 정렬 | 사내 A | 이 레포는 Toss 쿨 그레이 램프에 물려 있다 — `var(--grey-*)` 직접 참조 367회 + 램프에서 파생되는 역할 토큰(`--color-text-*` 379 / `--color-bg-*` 125 / `--color-border` 219) 723회 + 생 hex `#191f28`·`#e5e8eb` 17회 = **약 1,107곳**(`grep -o … src`, 2026-08-01). 온도를 바꾸면 전면 재작업이고 얻는 것이 없다 |
| **경고용 해저드 옐로** | 사내 B | ★이 과제의 함정. 이름만 같은 노랑이지 **의미가 정반대**다 — 그쪽 노랑은 "현장 위험 신호", 여기 노랑은 "노란봉투 = 연대·정체성"이다. 가져오면 사이트 자체의 경고 semantic(§3.3 warn)과 충돌한다. 명시적 기각. **단, 이 기각이 충돌을 없애지는 못한다** — 브랜드 노랑과 앰버 주의색은 인접 색상이라 §3.4가 채택한 값들끼리도 실측 명도차가 거의 없다(`--color-brand-surface #fef9c3` vs `--color-warn-bg #fef3c7` = 1.04:1). 대응은 §3.4 말미의 형태 분리 규칙과 §10 미결 |
| 전 토큰 각진 반경 + 섀도 전면 제거 | 사내 B | "현장 계기판" 미감. 긴 산문을 읽히는 화면과 맞지 않는다 |
| 히어로 초굵은 웨이트 + 강한 음수 자간 + 1 미만 행간 | 사내 B | 한글 자소 붕괴. §4 웨이트 래더 위반 |
| 스티키 좌패널 + 워크플로 스테퍼 | 사내 B | 작업 흐름 제품의 레이아웃. 여기는 문서 열람이다 |

---

## 3. 색

### 3.1 원시 램프 (globals.css 실재, 변경 금지)

| 램프 | 값 |
|---|---|
| grey-50 → 900 | `#f9fafb` `#f2f4f6` `#e5e8eb` `#d1d6db` `#b0b8c1` `#8b95a1` `#6b7684` `#4e5968` `#333d4b` `#191f28` |
| blue-50 → 700 | `#e8f3ff` `#c9e2ff` `#90c2ff` `#64a8ff` `#4593fc` `#3182f6` `#1b64da` `#1957c2` |

다크(`.dark`)에서 grey 램프는 **재정의되지만 정확한 반전은 아니다.** 양끝(50↔900)만 맞바뀌고 중간은 새 값이다:

| 단계 | 50 | 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800 | 900 |
|---|---|---|---|---|---|---|---|---|---|---|
| 라이트 | `#f9fafb` | `#f2f4f6` | `#e5e8eb` | `#d1d6db` | `#b0b8c1` | `#8b95a1` | `#6b7684` | `#4e5968` | `#333d4b` | `#191f28` |
| 다크 | `#191f28` | `#1e2530` | `#2a3140` | `#3a4250` | `#6b7684` | `#8b95a1` | `#b0b8c1` | `#d1d6db` | `#e5e8eb` | `#f2f4f6` |

`--grey-300`의 다크값 `#3a4250`은 라이트 램프에 없는 신규값이고, `--grey-500` `#8b95a1`은 양쪽이 같으며, `--grey-100/200/300`의 다크값은 라이트 `800/700/600`의 반전이 아니다. **"반전된다"고 믿고 `--grey-300`을 쓰면 예상 밖 값이 나온다.** blue 램프는 `--blue-50`만 `rgba(49,130,246,0.12)`로 바뀌고 나머지 7단계는 그대로다.

### 3.2 역할 토큰 — 표면·잉크·경계

| 역할 | 토큰 | 라이트 | 다크 | 쓰는 곳 |
|---|---|---|---|---|
| 페이지 바탕 | `--color-bg-primary` | `#f9fafb` (grey-50) | `#0f1117` | `<body>`. 카드 배경으로 쓰지 않는다 |
| 카드·표면 | `--color-bg-surface` | `#ffffff` | `#191f28` | 카드, 표, 사이드바, 입력 필드 |
| 부양 표면 | `--color-bg-elevated` | `#ffffff` | `#1e2530` | 드롭다운, 팝오버, 모달 |
| 본문·제목 | `--color-text-primary` | `#191f28` (grey-900) | `#f2f4f6` | h1~h4, 본문 |
| 보조 텍스트 | `--color-text-secondary` | `#6b7684` (grey-600) | `#b0b8c1` | 설명문, 메타, 비활성 네비. **흰 카드 위에서만**(↓) |
| 3차 텍스트 | `--color-text-tertiary` | `#b0b8c1` (grey-400) | `#6b7684` | **비콘텐츠 전용** — 플레이스홀더, 비활성 아이콘(↓) |
| 경계선 | `--color-border` | `#e5e8eb` (grey-200) | `#2a3140` | 카드 윤곽, 구분선, 입력 보더 |
| 약한 경계 | `--color-border-subtle` | `rgba(0,0,0,.06)` | `rgba(255,255,255,.06)` | 표 행 구분. **현재 참조 0회** — 표 규격(§6.5)에서 살린다 |

원칙: 카드는 **배경색이 아니라 표면(흰색) + 1px 경계선**으로 구분한다. 배경색으로 블록을 나누지 않는다.

**★ 회색 잉크의 대비 한계 — 실측 (신규 규율).** 이 램프는 Toss 팔레트를 그대로 들여온 것이라 아래 두 토큰이 AA 경계에 걸린다.

| 토큰 | on `#ffffff` (카드) | on `#f9fafb` (페이지 바탕) | 판정 |
|---|---|---|---|
| `--color-text-secondary` `#6b7684` | 4.62:1 | **4.42:1** | 카드 위 통과 / **페이지 바탕 위 미달** |
| `--color-text-tertiary` `#b0b8c1` | **2.01:1** | **1.92:1** | **어느 표면에서도 텍스트로 못 씀** |
| (대안) `--grey-700` `#4e5968` | 7.11:1 | 6.81:1 | 통과 |

규칙으로 고정한다:

> 1. **`--color-text-tertiary`는 콘텐츠에 쓰지 않는다.** 캡션·날짜·출처처럼 **읽으라고 놓은 글자**는 콘텐츠다. 특히 법령 정보 사이트에서 갱신일·시행일은 신뢰성의 핵심 정보이므로 `--color-text-secondary` 이상을 쓴다. 이 토큰은 플레이스홀더·비활성 아이콘 등 **읽지 않아도 되는 것**에만 남긴다.
> 2. **`--color-text-secondary`는 흰 표면(`--color-bg-surface`) 위에서만 쓴다.** 페이지 바탕(`#f9fafb`) 위 보조 텍스트와 `--text-xs`(11~12px) 텍스트는 `--grey-700` `#4e5968`을 쓴다.
> 3. 다크에서도 `--color-text-tertiary`(`#6b7684`)는 `#0f1117` 위 4.09:1 / `#191f28` 위 3.59:1로 같은 제약을 받는다.

이 규율은 §6.4(배지 중립 잉크 = `--grey-700`)와 §6.5(표 헤더)에 동일하게 적용된다 — 같은 조건을 절마다 다르게 판정하지 않는다.

### 3.3 역할 토큰 — 인터랙션·의미색

| 역할 | 토큰 | 라이트 | 다크 | 쓰는 곳 |
|---|---|---|---|---|
| 인터랙션 액센트 | `--color-accent` | `#3182f6` (blue-500) | `#4593fc` (blue-400) **(신규)** | **비텍스트 UI 전용** — 포커스 링, 활성 보더, 아이콘, 활성 표시. **링크 텍스트·솔리드 CTA 배경으로 쓰지 않는다**(↓ ★★) |
| 액센트 잉크·솔리드 | `--color-accent-ink` **(신규 토큰)** | `#1b64da` (blue-600) | `#4593fc` (blue-400) | 본문 링크 텍스트, 흰(라이트)/어두운(다크) 잉크를 얹는 주 CTA 배경 |
| 액센트 hover | `--color-accent-hover` | `#1b64da` (blue-600) | `#64a8ff` (blue-300) **(신규)** | **`--color-accent`를 쓰는 비텍스트 요소의 hover 전용** |
| 액센트 잉크 hover | `--color-accent-ink-hover` **(신규 토큰)** | `#1957c2` (blue-700) | `#64a8ff` (blue-300) | `--color-accent-ink`를 쓰는 링크·주 CTA의 hover |
| 액센트 연한 배경 | `--color-accent-light` | `#e8f3ff` (blue-50) | `rgba(49,130,246,.12)` | **활성 네비 배경 전용.** 인용 블록·참고 배너는 `--color-info-bg`를 쓴다(§6.6) — 라이트에서는 두 값이 같지만 다크에서 `.12` vs `.16`으로 갈리므로 용도를 겹치지 않는다 |
| 성공 전경(솔리드·아이콘) | `--color-success` **(신규 토큰)** | `#059669` | `#34d399` **(신규)** | 체크 아이콘, 배너 좌측선. **흰 텍스트를 얹는 솔리드 배경으로 쓰지 않는다**(↓ ★★) |
| 성공 잉크(틴트 위 텍스트) | `--color-success-ink` **(신규 토큰)** | `#15803d` | `#6ee7b7` **(신규)** | 성공 배지·배너의 **텍스트** |
| 성공 배경 | `--color-success-bg` **(신규 토큰)** | `#dcfce7` | `rgba(5,150,105,.16)` **(신규)** | 성공 배지·배너 배경 |
| 성공 경계 | `--color-success-border` **(신규 토큰)** | `#a7f3d0` **(신규)** | `rgba(52,211,153,.32)` **(신규)** | 성공 배너 보더 |
| 주의 전경(솔리드·아이콘) | `--color-warn` **(신규 토큰)** | `#b45309` | `#fbbf24` **(신규)** | 주의 아이콘, 배너 좌측선 |
| 주의 잉크 | `--color-warn-ink` **(신규 토큰)** | `#92400e` | `#fcd34d` **(신규)** | 주의 배지·배너 텍스트 |
| 주의 배경 | `--color-warn-bg` **(신규 토큰)** | `#fef3c7` | `rgba(180,83,9,.18)` **(신규)** | 주의 배너 배경 |
| 주의 경계 | `--color-warn-border` **(신규 토큰)** | `#fde68a` | `rgba(251,191,36,.32)` **(신규)** | 주의 배너 보더 |
| 위험 전경(솔리드·아이콘) | `--color-danger` **(신규 토큰)** | `#dc2626` | `#f87171` **(신규)** | 위반 아이콘, 배너 좌측선. 흰 텍스트를 얹는 솔리드 배경(라이트 4.83:1 — 의미색 중 유일하게 통과) |
| 위험 잉크 | `--color-danger-ink` **(신규 토큰)** | `#b91c1c` | `#fca5a5` **(신규)** | 위반 배지·배너 **텍스트** |
| 위험 배경 | `--color-danger-bg` **(신규 토큰)** | `#fee2e2` | `rgba(220,38,38,.18)` **(신규)** | 위반 배지 배경 |
| 위험 경계 | `--color-danger-border` **(신규 토큰)** | `#fecaca` **(신규)** | `rgba(248,113,113,.32)` **(신규)** | 위반 배너 보더 |
| 정보 전경 | `--color-info` **(신규 토큰)** | `#1b64da` (blue-600) | `#64a8ff` (blue-300) | 참고 배너 좌측선·아이콘 |
| 정보 잉크 | `--color-info-ink` **(신규 토큰)** | `#1957c2` (blue-700) | `#90c2ff` (blue-200) | 참고 배지·배너 텍스트 |
| 정보 배경 | `--color-info-bg` **(신규 토큰)** | `#e8f3ff` (blue-50) | `rgba(49,130,246,.16)` **(신규)** | 참고 배너 배경 |

라이트 hex 대부분은 현행 코드에 이미 흩어져 있는 값을 승격시킨 것이다. 실측(`grep -o '#[0-9a-f]\{6\}' -r src`, 6자리만, 2026-08-01):

| 값 | src 전역 출현 | 주요 위치 |
|---|---|---|
| `#fef3c7` (warn-bg) | 17 | `CalloutBox.tsx:5`, `ContractCheckClient.tsx:30,98`, `ChecklistWidget`·`DeepChecklistWidget`·`SimpleChecklistWidget`, `StepDiagram.tsx:9`, `category-colors.ts:4` |
| `#92400e` (warn-ink) | 17 | 위와 같은 파일들의 텍스트 색 (단 `ContractCheckClient.tsx:29-34`는 **아님** — 그 자리의 risk 전경은 `#b45309`) |
| `#059669` (success) | 11 | `HomeClient.tsx:81`, `guide/page.tsx`(×3) 등 |
| `#dc2626` (danger) | 10 | `HomeClient.tsx:73` 등 |
| `#ecfdf5` | 7 | `HomeClient.tsx:82`, `guide/page.tsx`(×1) 등 |
| `#166534` | 6 | `CalloutBox.tsx:6`(tip 라벨) 등 |

`ContractCheckClient.tsx:29-34`의 `STATUS_STYLE`은 4페어 8개다 — `#fee2e2/#b91c1c`(violation), `#fef3c7/#b45309`(risk), `#dcfce7/#15803d`(ok), `#e2e8f0/#475569`(needs_data). 전부 AA 통과(5.30 / 4.51 / 4.57 / 6.15:1).

> `/guide` 라우트 자체(`src/app/guide/page.tsx`)의 6자리 hex는 **6개뿐**이다(`#059669`×3, `#ecfdf5`, `#166534`, `#ef4444`). 나머지는 `/guide`가 **렌더하는 공용 컴포넌트**, 특히 `src/components/CalloutBox.tsx`(콜아웃 3종의 hex 원산지)에 있다. 마이그레이션 대상 경로를 `src/app/guide/**`로 잡으면 실제 색은 하나도 안 잡힌다(§9 P3-2).

**★ 전경/잉크를 나누는 이유 — 대비.** 연한 틴트 배경 위에 `--color-*` 전경색을 그대로 얹으면 AA에 미달한다(`#059669` on `#ecfdf5` = 3.58:1, `#dc2626` on `#fee2e2` = 3.95:1). 배지 텍스트는 11~12px이라 더 불리하다. 그래서 **솔리드/아이콘용 색(`--color-*`)과 틴트 위 텍스트용 색(`--color-*-ink`)을 분리**한다. 사내 A도 같은 문제로 솔리드용 액센트와 링크용 액센트를 별도 토큰(`--k-accent-solid` / `--k-accent-link`)으로 갈라 뒀고, 같은 해법을 쓴다. (사내 A 쪽 실측 대비는 이 레포에서 검증할 수 없으므로 수치를 옮겨 적지 않는다.) `ContractCheckClient.tsx:29-34`가 이미 AA를 통과하는 페어를 쓰고 있으므로, 이 분리는 신규 규범이 아니라 **현행 정답의 토큰화**다.

**★★ 솔리드 배경 + 흰 잉크는 별도로 검증한다 (신규 규율).** 위 ★은 *틴트 위 텍스트*만 다뤘다. **반대 방향 — 진한 배경 위 흰 글자 — 도 같은 4.5:1 기준을 받는다.** 버튼 라벨은 15px/600이라 WCAG 대형 텍스트 예외(18.66px bold 이상)에 해당하지 않는다. 실측:

| 배경 | 흰 잉크 대비 | 판정 | 처분 |
|---|---|---|---|
| `#dc2626` (danger, 라이트) | 4.83:1 | 통과 | 그대로 |
| `#059669` (success, 라이트) | **3.77:1** | 미달 | 성공은 **솔리드 배경으로 쓰지 않는다.** 필요하면 `#047857`(emerald-700, 5.48:1) **(신규)** |
| `#3182f6` (accent, 라이트) | **3.71:1** | 미달 | 주 CTA 배경은 `--color-accent-ink` `#1b64da`(5.41:1)로 **(신규)** |
| `#4593fc` (accent, 다크) | **3.07:1** | 미달 | 다크 주 CTA는 배경 `#4593fc` + 잉크 `#0f1117`(6.14:1). `.dark`의 `--primary-foreground: #0f1117` 선례와 일치 |
| `#64a8ff` (accent-hover, 다크) | **2.45:1** | 미달 | hover에도 어두운 잉크를 유지한다 |
| `#facc15` (brand) | — | — | 잉크가 흰색이 아니다. `#191f28` on `#facc15` = 10.81:1 통과 |

danger만 통과하고 success·accent가 미달하는 비선택적 분포이므로, 이것은 기준을 낮게 잡은 것이 아니라 **검증을 빠뜨린 것**이다. 아래를 규칙으로 고정한다:

> **흰 텍스트를 얹을 수 있는 솔리드 배경은 라이트 기준 대비 4.5:1을 넘는 색뿐이다.** 넘지 못하면 (a) 더 어두운 단계를 쓰거나 (b) 잉크를 어두운 중성색으로 바꾼다. 다크에서는 액센트·의미색이 전부 밝은 단계이므로 **잉크가 어두워진다** — 라이트의 흰 잉크 규칙을 다크에 그대로 복사하지 않는다(다크 `--color-success` `#34d399`에 흰 글자를 얹으면 약 1.8:1이다).

**★ 링크 색 — 본문 링크는 `--color-accent`가 아니다 (신규 규율).** `#3182f6`은 흰 카드 위 3.71:1, 페이지 바탕(`#f9fafb`) 위 **3.55:1**로 본문 텍스트 기준에 미달한다. `var(--color-accent)` 참조 215곳 중 링크 텍스트에 해당하는 것은 전부 `--color-accent-ink` `#1b64da`(5.41 / 5.18:1)로 옮긴다. 다크는 `#4593fc`가 `#0f1117` 위 6.14:1 / `#191f28` 위 5.39:1로 통과하므로 그대로 둔다. 밑줄 규칙은 §6.8.

**★ 파랑이 4단계로 늘어난 이유 — hover 토큰을 두 갈래로 나눈다.** `--color-accent-ink`를 신설하면서 라이트 기준 `#1b64da`가 `--color-accent-hover`와 같은 값이 되었다. 그대로 두면 **링크 hover가 라이트에서 아무 변화도 일으키지 않는다**(둘 다 `#1b64da`). 다크는 `#4593fc` → `#64a8ff`로 정상 작동하므로, 정본인 라이트에서만 깨지는 형태다. 따라서 hover를 용도별로 가른다:

| 기본 | hover | 대상 |
|---|---|---|
| `--color-accent` (blue-500 / 다크 blue-400) | `--color-accent-hover` (blue-600 / 다크 blue-300) | 포커스 링, 보더, 아이콘 등 **비텍스트** |
| `--color-accent-ink` (blue-600 / 다크 blue-400) | `--color-accent-ink-hover` (blue-700 `#1957c2` 6.60:1 / 다크 blue-300) | **링크 텍스트, 주 CTA 배경** |

네 값 전부 기존 blue 램프의 단계이므로 새 hex는 하나도 늘지 않는다 — 늘어나는 것은 이름뿐이다. `#1957c2`는 `--color-info-ink`와 값이 같지만 **용도가 다르므로 토큰을 공유하지 않는다**(§3.5의 "세 번째 위험색을 만들지 말 것"은 *같은 용도에 토큰을 중복 생성하지 말라*는 뜻이며, 값이 겹치는 것과는 다른 문제다).

**의미색 규율(사내 A에서 이식):** 의미색은 **글리프·배지·배너 보더**로만 말한다. 카드 배경 전체를 성공색으로 칠하지 않는다. 배너 배경은 예외적으로 허용하되 §6.6 규격을 따른다.

### 3.4 노랑 헌장 (Yellow Charter)

**판단 근거.** 파랑은 제거할 수 없다 — `var(--color-accent)` 참조 215회, `/tools`를 뺀 전 화면의 링크·CTA가 여기 물려 있다. 노랑도 제거할 수 없다 — 사이트 이름이 노란봉투법이고 `/tools` 전체가 노랑으로 돌아간다. 따라서 이 사이트는 **역할이 분리된 2액센트 체계**이고, 노랑은 지금까지 토큰 없이 방치되었을 뿐 버그가 아니다. 아래로 고정한다.

| | 파랑 `--color-accent` | 노랑 `--color-brand-*` |
|---|---|---|
| 역할 | 인터랙션 신호("누르면 이동한다") | 브랜드 정체성 + 도구 화면 표면 |
| **허용** | 본문 링크, 포커스 링, 주 CTA(정보 화면), 활성 네비, 인용 블록 좌측선 | `/tools` 카드 hover 보더, 도구 CTA 배경, 아이콘 칩 배경, 선택 상태 보더/배경, 브랜드 마크 |
| **금지** | `/tools` 도구 CTA 배경 | **흰 배경 위 텍스트 색**, 링크 색, 포커스 링, 본문 강조, 의미색(경고) 대용 |

**하드 제약 — 대비.** `#facc15`(brand-400)는 흰 배경 위 텍스트로 대비 미달이다. 규칙으로 고정한다:

> **노랑은 표면 색이다. 노랑 표면 위 잉크는 항상 어두운 중성색(`#191f28`)이다.**
> **라이트에서** 노랑을 텍스트 색으로 쓸 때는 밝은 표면 위 대비를 위해 brand-700(`#a16207`, 흰 배경 4.92:1) 이하 명도만 쓴다.
> **다크에서는 방향이 뒤집힌다** — 어두운 표면 위에서는 brand-300(`#fde047`) 같은 밝은 단계라야 대비가 나온다. 아래 `--color-brand-ink`의 다크값이 밝은 이유가 이것이며, 위 명도 상한은 라이트에만 적용된다.

현행 코드는 이미 이를 지키고 있다 — `bg-yellow-400 text-slate-900`(표면 + 어두운 잉크), `text-yellow-700`(명도 통과). 이 규칙은 새 규범이 아니라 관행의 성문화다.

**노랑 램프** (신규 토큰. 값은 Tailwind yellow 스케일 — 현행 코드가 이미 이 값들을 직타로 쓰고 있어 매핑이 1:1이다)

| 토큰 | 값 | 현행 코드의 출처 |
|---|---|---|
| `--brand-50` | `#fefce8` | `HomeClient.tsx` 아이콘 칩 배경 |
| `--brand-100` | `#fef9c3` | 기존 `--yellow-100` 승계 (globals.css:94) |
| `--brand-200` | `#fef08a` **(신규)** | — |
| `--brand-300` | `#fde047` | `tools/page.tsx:85` `bg-yellow-300` |
| `--brand-400` | `#facc15` | `HolidayPayCalculator.tsx:36,346,441,478,519` 생 hex, `bg-yellow-400` |
| `--brand-500` | `#eab308` **(신규)** | — |
| `--brand-600` | `#ca8a04` | `HomeClient.tsx` 아이콘 전경 |
| `--brand-700` | `#a16207` | `text-yellow-700` (`tools/page.tsx:95` 등) |
| `--brand-800` | `#854d0e` | `ContractCheckClient.tsx:189` |

**노랑 역할 토큰**

| 역할 | 토큰 | 라이트 | 다크 | 쓰는 곳 |
|---|---|---|---|---|
| 브랜드 표면(강) | `--color-brand-solid` **(신규)** | `#facc15` | `#facc15` (동일) | 도구 주 CTA 배경. 위 잉크는 항상 `#191f28` |
| 브랜드 표면(약) | `--color-brand-surface` **(신규)** | `#fef9c3` | `rgba(250,204,21,.16)` **(신규)** | 아이콘 칩, 선택 카드 배경 |
| 브랜드 경계 | `--color-brand-border` **(신규)** | `#facc15` | `rgba(250,204,21,.45)` **(신규)** | 카드 hover 보더, 선택 상태 보더 |
| 브랜드 잉크 | `--color-brand-ink` **(신규)** | `#854d0e` | `#fde047` **(신규)** | 노랑 문맥의 텍스트(연한 노랑 배경 위) |

기존 `--yellow-100`은 `--brand-100`으로 이름을 바꾸고, 다크값 `#422006`은 폐기한다(새 다크 규칙은 알파 오버레이).

**★ 브랜드 노랑 ↔ 주의 앰버 충돌 — 형태로 가른다 (신규 규율).** §2.2가 사내 B 해저드 옐로를 기각한 근거("브랜드 노랑이 warn semantic과 충돌한다")는 값을 바꿔도 사라지지 않는다. 실측 명도차:

| 페어 | 대비 |
|---|---|
| `--color-brand-surface` `#fef9c3` vs `--color-warn-bg` `#fef3c7` | **1.04:1** |
| `--brand-50` `#fefce8` vs `#fef3c7` | **1.08:1** |
| `--color-brand-border` `#facc15` vs `--color-warn-border` `#fde68a` | **1.23:1** |

색으로는 구분이 불가능하다. 그렇다고 warn을 다른 색상으로 옮기면 `#fef3c7`/`#92400e` 페어를 쓰는 현행 17곳(AA 6.37:1 통과)을 전부 후퇴시킨다. 따라서 **색이 아니라 형태로 가른다:**

> 1. **주의(warn)는 항상 `border-left: 4px solid var(--color-warn)`을 단다.** 좌측 굵은 선이 없으면 주의가 아니다.
> 2. **브랜드 표면은 좌측선을 갖지 않는다.** 대신 `1px solid var(--color-brand-border)` 전체 테두리 + 어두운 중성 잉크(`#191f28`)로 말한다.
> 3. **같은 블록 안에서 브랜드 표면과 주의 배너를 인접 배치하지 않는다.**
> 4. 근본 해소(브랜드 노랑 또는 주의 앰버 중 하나의 색상 이동)는 §10 미결로 남긴다.

### 3.5 폐기 대상 토큰

| 토큰 | 처분 | 이유 |
|---|---|---|
| `--chart-1` ~ `--chart-5` | 재정의 필요 | blue-500/400/300/600/700 단색 5단계라 범주형 데이터에서 인접 계열이 구분 안 된다. `/stats`가 recharts 자체 색을 쓰는 것이 그 증거. 범주형 팔레트 지정은 **별건**으로 남긴다 |
| `--transition-slow` (500ms) | 삭제 | 참조 0회 |
| `--color-border-subtle` | **유지·활성화** | 참조 0회지만 §6.5 표 행 구분에서 쓴다 |
| `--destructive` (라이트 `#dc2626` / 다크 `#ef4444`) | **존치, 용도 한정** | shadcn / `decisions-ui` 레이어 전용. **단일 값이 아니다** — `.dark`(globals.css:177)에서 `#ef4444`로 재정의되므로 신규 `--color-danger`(라이트 `#dc2626` / 다크 `#f87171`)와 **라이트에서만 일치하고 다크에서 갈린다.** 두 레이어가 한 화면에 같이 나오면 빨강이 두 톤으로 보인다. **신규 화면은 `--color-danger*`만 쓴다.** 세 번째 위험색 토큰을 만들지 말 것 |
| `@font-face "Pretendard Variable Fallback"` (globals.css:5-9) | 삭제 | 그 패밀리명을 참조하는 선언이 하나도 없다 |
| `src/components/ui/*` (5개) | 삭제 | 임포트 0회. `decisions-ui`와 중복된 죽은 레이어 |

---

## 4. 타이포

### 4.1 폰트 스택

```
"Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont, system-ui,
Roboto, "Helvetica Neue", "Segoe UI", "Apple SD Gothic Neo", "Noto Sans KR",
"Malgun Gothic", sans-serif
```

Pretendard Variable v1.3.9를 jsDelivr에서 preconnect(`layout.tsx:67`) + preload(`:68-73`) + stylesheet(`:74-78`)로 로드한다. `next/font`는 쓰지 않는다.

> ★ **현행 버그 (정적 독해 — 런타임 미검증).** `globals.css:14`의 `--font-sans`는 스택 **선두에 `var(--font-pretendard)`**를 두는데, 이 변수는 레포 어디에도 정의돼 있지 않다(`grep -rn 'font-pretendard' src` → globals.css:14 단 1건, `next/font` 0건 — **이 두 사실은 확인됨**). CSS 사양상 정의되지 않은 `var()`가 폴백 없이 오면 그 선언은 invalid-at-computed-value-time으로 처리되어 상속값 또는 초기값으로 떨어진다.
>
> **다만 "그래서 화면이 Pretendard 없이 렌더된다"는 결과 서술은 아직 실행으로 확인하지 않았다.** `@theme inline` 안의 선언이라 Tailwind가 빌드 타임에 어떻게 전개하는지에 따라 결과가 달라질 수 있고, 만약 정말 폴백으로 떨어졌다면 운영 사이트가 지금까지 한국어 폴백 없이 서비스되어 왔다는 뜻이라 육안으로 드러났어야 한다. **수정 자체는 무해하므로 P0로 두되(= `var(--font-pretendard), ` 제거), 수정 전후로 `getComputedStyle(document.body).fontFamily`를 실제 브라우저에서 확인하고 기록한다.** (§9 P0-1)

한글 본문은 **단일 폰트 패밀리**로 간다. mono는 코드 블록·라틴 라벨에만 쓰고 한글을 mono로 렌더하지 않는다.

### 4.2 스케일

크기는 전부 `clamp()` 유동 스케일이다(globals.css:85-91). weight·line-height·letter-spacing은 이 문서에서 처음 규정한다.

| 용도 | 토큰 | px (최소→최대) | weight | line-height | letter-spacing |
|---|---|---|---|---|---|
| 페이지 히어로 h1 | `--text-hero` | 28 → 80 | 700 **(신규)** | 1.15 **(신규)** | -0.02em **(신규)** |
| 섹션 제목 h2 | `--text-2xl` | 24 → 32 | 700 **(신규)** | 1.3 **(신규)** | -0.02em **(신규)** |
| 하위 제목 h3 | `--text-xl` | 20 → 24 | 600 **(신규)** | 1.4 **(신규)** | -0.015em **(신규)** |
| 카드 제목 h4 | `--text-lg` | 18 → 20 | 600 **(신규)** | 1.45 **(신규)** | -0.01em **(신규)** |
| 본문 | `--text-base` | 15 → 17 | 400 | **1.65** | **-0.01em** |
| 보조·설명 | `--text-sm` | 13 → 14 | 400 (라벨은 500) **(신규)** | 1.6 **(신규)** | -0.01em **(신규)** |
| 캡션·배지 | `--text-xs` | 11 → 12 | 500 **(신규)** | 1.5 **(신규)** | 0 **(신규)** |

본문 `1.65` / `-0.01em`은 `.content-body`(globals.css:283-291)에 실재하는 값이다. **`body`에는 걸려 있지 않다** — `body`(globals.css:240-249)의 선언은 `background-color`·`background-image`·`background-attachment`·`font-family`·`color` 다섯 개뿐이다. 즉 이 값은 `.content-body`를 붙인 요소에만 적용되며, 전역 기본값이 아니다(§4.3 전달 기전 참조).

**전달 기전 (신규).** 위 7단계의 weight·line-height·letter-spacing을 담을 클래스가 현재 없다. 개발자가 제목마다 3속성을 손으로 반복하지 않도록, globals.css에 아래 7개 클래스를 만들고 **크기·weight·LH·LS를 한 묶음으로** 제공한다(§9 P5-10).

| 클래스 | 대응 |
|---|---|
| `.t-hero` / `.t-h2` / `.t-h3` / `.t-h4` | 각각 hero / 2xl / xl / lg 행 전체 |
| `.t-body` | `.content-body`를 이 이름으로 통합(기존 클래스 유지, 별칭) |
| `.t-sm` / `.t-xs` | sm / xs 행 전체 |

이 클래스들이 생기기 전까지는 §4.3의 "인라인 `var(--text-*)`"가 크기만 통일하는 **과도기 규칙**이다.

### 4.3 규율

| 규칙 | 내용 |
|---|---|
| 웨이트 래더 | **400 / 500 / 600 / 700만.** 사이값 금지, 300 이하 전면 금지(한글 자형 붕괴), 800 이상 금지 |
| 강조 | 굵게보다 **크게**. 위계가 모자라면 weight를 올리지 말고 스케일을 한 단계 올린다 |
| 트래킹 상한 | 음수 트래킹은 **-0.02em까지**. 그 이상 조이면 한글 자소가 붙는다. 양수 트래킹은 라틴 대문자 라벨(`tracking-wide`)에만 |
| 행간 하한 | 본문은 **1.6 미만 금지**. 밀집 표만 1.5까지 허용. **현재 이를 강제하는 전역 기전이 없다** — `body`에 `line-height` 선언이 없어 브라우저 기본값(≈1.2)이 적용되는 화면이 남는다 |
| 줄바꿈 | `word-break: keep-all` + `overflow-wrap: break-word`. **현행은 `.content-body`(globals.css:287-288)와 `.blog-content`(570-571)에만 있고 `body` 전역이 아니다.** 이 두 클래스를 안 쓰는 화면은 미적용 상태다 → **`body`로 승격한다 (신규, §9 P5-11)** |
| 본문 최대폭 | `38em`(한글 약 40자/줄). **`.content-body`(globals.css:289)에만 실재하고 `.blog-content`(566-572)에는 `max-width` 선언 자체가 없다** → `.blog-content`에도 부여한다. 아울러 `.blog-content`는 `font-size: 15px` 고정(유동 `--text-base` 미사용) / `line-height: 1.75`(본문 규격 1.65와 불일치)이므로 함께 정렬한다 **(신규, §9 P5-11)** |
| 본문 폭 vs 컨테이너 폭 | **둘 다 건다.** 컨테이너는 `max-w-[820px]`(§5.2), 그 안의 산문 블록은 `38em`(≈610~650px). 남는 170~210px은 여백이 아니라 **인용 배너·표·이미지가 본문보다 넓게 나가는 자리**다. 산문 블록은 컨테이너 안에서 **좌측 정렬**(중앙 정렬 금지 — 왼쪽 시작선이 흔들리면 긴 글에서 읽기가 무너진다) |
| 제목 크기 지정 | h1~h4는 **§4.2의 `.t-*` 클래스로 통일.** 클래스 도입 전 과도기에는 인라인 `var(--text-*)`를 쓴다. Tailwind `text-2xl` 등으로 우회 금지 — 현재 `/faq`만 우회 중이라 같은 h1이 페이지마다 다른 크기다 |
| 제목 색 | **`var(--color-text-primary)` 하나로 통일.** 현재 `var(--grey-900)` / `var(--color-text-primary)` / `#0f172a` / `#f2f4f6` 4가지로 갈려 있고, 생 hex 2종은 다크에서 반전되지 않는다 |
| 24px 초과 | **전용 CSS 클래스(`.t-hero` / `.t-h2`)로만.** Tailwind 유틸에 의존하지 말 것 — 프리셋이 `text-3xl` 이상을 생성하지 않으면 16px로 폴백한다(사내 A 실측 사례). `--text-2xl`(24→32px)과 `--text-hero`가 여기 해당하며, §2.1의 "24px 초과는 전용 클래스" 규율과 일치시킨 것이다. 인라인 `var(--text-*)`는 24px 이하 단계와 클래스 도입 전 과도기에만 허용 |
| 임의 크기 금지 **(신규)** | `text-[10px]` 같은 Tailwind 임의값으로 스케일 밖 크기를 만들지 않는다. 하한은 `--text-xs`(11px)다. 현재 `faq/FaqClient.tsx:280`이 `text-[10px]`로 이를 밑돈다 |

---

## 5. 간격 · 반경 · 그림자

### 5.1 간격

4px 베이스(`--spacing: .25rem`, Tailwind v4 기본). 별도 토큰을 만들지 않고 Tailwind 스케일을 쓴다.

| 용도 | 값 | 상태 |
|---|---|---|
| 인접 요소 | 8px (`gap-2`) | 현행 |
| 그룹 내부 | 12~16px (`gap-3` / `gap-4`) | 현행 |
| 카드 간 | 16px (`gap-4`) | 현행 |
| 카드 내부 패딩 | **20px 모바일 / 24px 데스크톱 (`p-5 sm:p-6`)** | **(신규 통일)** — 현재 p-4/p-5/p-6/p-7 4종 혼재 |
| 섹션 상하 | 40px (`py-10`) | 현행(`src/app` 내 20개 파일) |
| 히어로 상하 | 56 / 80 / 128px (`py-14 sm:py-20 md:py-32`) | 현행(홈 전용) |
| 터치 타깃 하한 | **44 × 44px** | **(신규)** — 아래 예외 규정 참조 |

**터치 타깃 44px의 적용 범위 (신규 — 문서 내 충돌 해소).** 이 하한을 무조건 걸면 §6.2 ghost 버튼(`p-2`)과 §6.7 필터 칩(`px-3 py-1.5` + `--text-xs` → 실측 높이 약 30px)이 규칙 위반 상태로 고정된다. 아래로 가른다:

| 대상 | 규칙 |
|---|---|
| 독립 행동 요소(버튼 3변형, 폼 입력, FAB, 네비 링크) | `min-height: 44px` **필수** |
| 아이콘 전용 버튼(ghost) | `min-width/height: 44px` 필수. 아이콘은 20px, 나머지는 패딩으로 채운다(`p-2`만으로는 미달) |
| 조밀 제어 요소(필터 칩, 표 내부 링크, 배지형 태그) | 시각 높이는 30px까지 허용하되 **`min-height: 44px`의 투명 히트 영역**을 확보한다(`::before` 확장 또는 `py-` 증가 후 시각 높이를 line-height로 흡수). 시각적으로 작아 보이는 것과 손가락에 작은 것은 다른 문제다 |

### 5.2 콘텐츠 최대폭 — 3단으로 통일 (신규)

현재 `src/**/*.tsx`의 `max-w-*` 변형은 **25종**이다(실측: `1100`×11, `3xl`×7, `6xl`×4, `4xl`×4, `760`×4, `820`×3, `800`×3, `700`×3, `1200`×3, `xl`×2, `none`×2, `md`×2, `980`×2, `900`×2, `560`×2, `1400`×2, `lg`, `7xl`, `920`, `860`, `85%`, `80%`, `400`, `280`, `1000`). 페이지를 옮길 때마다 좌우 여백이 튄다. 아래 3개만 쓴다(`max-w-[85%]`·`max-w-[280px]` 같은 **컴포넌트 내부 폭 제한은 대상 밖**이다 — 이 표는 페이지 셸/콘텐츠 컨테이너에만 적용한다).

| 폭 | 용도 | 배정 화면 |
|---|---|---|
| `max-w-[1400px]` | 셸(네비·푸터) — **현행 유지** | 전역 레이아웃 |
| `max-w-[1100px]` | 목록·그리드·데이터 화면 **(신규 통일)** | `/`, `/faq`, `/search`, `/tools` 인덱스, `/stats`, `/decisions`, `/sanction`, `/database`, `/news`, `/blog` 인덱스 |
| `max-w-[820px]` | 읽는 화면 **(신규 통일)** | `/guide`, `/manual`, `/blog/[slug]`, `/cases/[id]`, `/harassment`, `/checklist`, `/subsidy`, 도구 폼(`/tools/*`), `/contact`·`/privacy`·`/terms` |

넓은 표를 쓰는 화면(`/stats`, `/decisions`)은 1100단이며, 표가 넘치면 폭을 키우지 않고 §6.5의 `overflow-x: auto`로 처리한다.

### 5.3 반경

| 단계 | 값 | 용도 |
|---|---|---|
| `--radius-sm` | 6px (`calc(--radius * 0.6)`) | 배지, 칩, 작은 태그 |
| `--radius-md` | 8px (`calc(--radius * 0.8)`) | 버튼, 입력 필드, 세그먼트 |
| `--radius` (lg) | 10px | 배너, 중간 크기 요소. **표는 카드 규격을 따르므로 여기 해당하지 않는다**(§6.5 → §6.1 = `--radius-xl` 14px) |
| `--radius-xl` | 14px (`calc(--radius * 1.4)`) | **카드 기본** |
| `rounded-full` | 999px | FAB, 필터 칩, 아바타 |

**카드 반경은 `rounded-xl`(14px) 단일로 통일한다 (신규 결정).** 현재 `rounded-lg`(10) / `xl`(14) / `2xl`(16)이 뒤섞여 있다. `2xl`을 버리고 `xl`을 택한 이유는 `--radius-xl`이 토큰 파생값이고 `2xl`(1rem)은 토큰 체계 밖 Tailwind 기본값이기 때문이다.

**하드코딩 반경 정리 (신규):** `.nav-cta` / `.nav-link`의 `border-radius: 8px`는 `var(--radius-md)`로, `.glass-panel`의 16px·`.glass-elevated`의 20px·`.nav-dropdown`의 14px는 각각 `var(--radius-xl)`로 치환한다. 현재 네비 CTA(8px)와 본문 CTA(10px)의 모서리가 다르다.

**보더 굵기 (신규):** 카드 보더는 **1px 단일**. 선택 상태 표현은 굵기가 아니라 **색(`--color-brand-border`) + 배경(`--color-brand-surface`)**으로 한다. 현재 `border-2`는 **6개 파일 10회**로, `/tools` 계열(`tools/page.tsx:71`, `ContractCheckClient.tsx:309`, `PhotoUploadCard.tsx:162`, `HolidayPayCalculator.tsx:439,476,517,548,662`)뿐 아니라 `ai/page.tsx:10`(스피너)·`guide/page.tsx:138`(타임라인 점)에도 있다. 뒤의 둘은 카드 보더가 아니라 **형태 요소**이므로 이 규칙의 대상이 아니다 — 대상은 카드·선택 카드 8회다.

### 5.4 그림자

| 토큰 | 값 (라이트) | 용도 |
|---|---|---|
| `--shadow-sm` | `0 1px 2px rgba(0,0,0,.05), 0 0 0 1px rgba(0,0,0,.03)` | 카드 기본 |
| `--shadow-md` | `0 4px 6px -1px rgba(0,0,0,.07), 0 2px 4px -1px rgba(0,0,0,.04), 0 0 0 1px rgba(0,0,0,.03)` | 카드 hover |
| `--shadow-lg` | `0 10px 15px -3px rgba(0,0,0,.08), …` | 드롭다운, FAB, 팝오버 |
| `--shadow-xl` | `0 20px 40px -8px rgba(0,0,0,.1), 0 8px 16px -4px rgba(0,0,0,.06)` | 모달만 |

다크 실값(globals.css:157-160) — **단순 알파 상향이 아니다.** `md`/`lg`/`xl`은 라이트에 있던 `0 0 0 1px` 링 레이어를 **잃는다**:

| 토큰 | 다크 실값 |
|---|---|
| `--shadow-sm` | `0 1px 2px rgba(0,0,0,.3), 0 0 0 1px rgba(255,255,255,.03)` (링이 흰색으로 바뀜) |
| `--shadow-md` | `0 4px 6px -1px rgba(0,0,0,.4), 0 2px 4px -1px rgba(0,0,0,.2)` (링 없음, 알파 `.2` 포함) |
| `--shadow-lg` | `0 10px 15px -3px rgba(0,0,0,.4)` (2레이어 → 1레이어) |
| `--shadow-xl` | `0 20px 40px -8px rgba(0,0,0,.5)` (2레이어 → 1레이어) |

즉 다크에서 `--shadow-md` 이상은 **경계선 역할을 하는 링이 사라진다.** 다크 카드가 hover에서 윤곽을 잃지 않으려면 `border`를 그림자에 의존시키지 말고 명시적으로 유지해야 한다.

**규율 (신규):**
1. 그림자는 위계가 아니라 **"떠 있는 것"**의 표시다. 버튼·배지·표 행에 그림자를 주지 않는다.
2. 카드 hover는 `--shadow-md` + `translateY(-2px)`. 현행 `.feature-card:hover`의 `--shadow-xl` + `translateY(-4px)`는 과하다 — xl은 모달 전용으로 내린다.
3. **하드코딩 그림자 금지.** 우하단 FAB의 `box-shadow: 0 4px 16px rgba(0,0,0,0.15)`는 `var(--shadow-lg)`로 치환한다. **수정 지점은 `src/components/FloatingChatButton.tsx:20` 한 곳이다** — 이 컴포넌트는 `layout.tsx:154`에서 전역 1회 마운트되고 `/ai`에서만 자기 자신을 숨긴다. "7페이지에 각각 하드코딩돼 있다"는 뜻이 아니다.
4. `/tools` 계열은 현재 인라인 토큰 그림자 0건이다. 카드에 `--shadow-sm`을 붙여 다른 화면과 맞춘다.
5. **`--shadow-sm`은 `0 0 0 1px rgba(0,0,0,.03)` 링을 포함하므로, 1px `border`와 함께 쓰면 경계선이 이중으로 그려진다.** §6.1 카드는 보더가 주 경계이고 `--shadow-sm`은 미세한 부양감만 담당한다 — 링 레이어가 보더와 겹쳐 보이면 `--shadow-sm`을 빼고 보더만 남긴다. 둘 중 **보더가 우선**이다(§3.2 원칙).

### 5.5 모션

| 토큰 | 값 | 용도 |
|---|---|---|
| `--transition-fast` | 150ms | 색·배경 전환, hover |
| `--transition-normal` | 300ms | 이동·크기 변화, 아코디언 |
| `--ease-expo-out` | `cubic-bezier(.16, 1, .3, 1)` | 위 둘의 기본 이징 |

버튼 hover의 `scale-105`는 **`translateY(-1px)`로 대체한다 (신규)** — 텍스트가 흐려지고 44px 타깃 경계가 흔들린다. `prefers-reduced-motion: reduce`에서 이동·스케일 애니메이션을 끈다 **(신규)**.

---

## 6. 컴포넌트 규격

모든 규격은 라이트/다크 양쪽을 **토큰으로** 지정한다. 토큰을 거치면 다크는 자동으로 따라온다 — 생 hex를 쓰는 순간 다크가 깨진다.

> ⚑ **선행 조건 — 이 절의 토큰은 아직 globals.css에 없다.** `--color-success/warn/danger/info` 계열 15종, `--color-accent-ink`, `--color-brand-*` 4종, `--brand-50~800` 램프는 전부 §3.3·§3.4가 **신규로 지정한 값**이며 `:root`(globals.css:44-130)에 정의 0건이다. **§9 P2-1·P3-1로 토큰을 먼저 신설하지 않고 이 절의 규격을 그대로 구현하면**, `background: var(--color-success-bg)` 같은 선언이 invalid-at-computed-value-time으로 떨어져 **배경이 아예 칠해지지 않는다.** 순서: P2-1·P3-1 → 이 절.
>
> 다크값을 생 hex로 적은 칸(§6.5 표 헤더 등)은 **대응 토큰이 있으면 토큰을 쓴다**는 뜻이며, 아래 표는 그 토큰의 실값을 병기한 것이다. 생 hex를 코드에 직접 쓰라는 지시가 아니다.

### 6.1 카드

| 속성 | 값 |
|---|---|
| 배경 | `var(--color-bg-surface)` — 라이트 `#ffffff` / 다크 `#191f28` |
| 보더 | `1px solid var(--color-border)` — 라이트 `#e5e8eb` / 다크 `#2a3140` |
| 반경 | `var(--radius-xl)` 14px |
| 패딩 | `p-5 sm:p-6` (20 / 24px) |
| 그림자 | `var(--shadow-sm)` — **보더가 주 경계이고 이것은 보조다**(§5.4-5). 링 레이어가 보더와 겹쳐 보이면 뺀다 |
| hover(클릭 가능한 카드만) | `var(--shadow-md)` + `translateY(-2px)`, `--transition-normal`(300ms — 이동이 있으므로 §5.5의 "이동·크기" 대역) |

클릭 불가능한 카드에는 hover 효과를 주지 않는다.

### 6.2 버튼

주 행동 버튼이 현재 4가지 규격으로 갈려 있다(홈 `px-6 py-3` / `/faq` `px-4 py-2` / `/tools` `bg-slate-900` + `bg-yellow-400` / 네비 `6px 16px`). 아래 4종으로 통일한다 **(신규 통일)**.

| 변형 | 배경 | 텍스트 | hover 배경 | 보더 | 크기 |
|---|---|---|---|---|---|
| **primary** (정보 화면 주 행동) | `var(--color-accent-ink)` — 라이트 `#1b64da` / 다크 `#4593fc` | 라이트 `#ffffff`(5.41:1) / **다크 `#0f1117`**(6.14:1) | `var(--color-accent-ink-hover)` — 라이트 `#1957c2`(6.60:1) / 다크 `#64a8ff` | 없음 | `--radius-md` 8px, `px-5 py-2.5`, min-height 44px, 15px/600 |
| **brand** (도구 화면 주 행동) | `var(--color-brand-solid)` `#facc15` (라·다 동일) | `#191f28` **고정**(10.81:1) | `var(--brand-500)` `#eab308` **(신규 지정)** — 잉크 유지 시 8.64:1 | 없음 | primary와 동일 |
| **secondary** | `var(--color-bg-surface)` | `var(--color-text-primary)` | `var(--grey-100)` | `1px solid var(--color-border)` | primary와 동일 |
| **ghost** (네비·아이콘) | 투명 | `var(--color-text-secondary)` | `var(--grey-100)` | 없음 | `--radius-md`, **아이콘 20px + `min-width/height: 44px`**(`p-2`만으로는 44px 미달 — §5.1 예외표) |

- **primary 배경이 `--color-accent`가 아닌 이유:** `#3182f6` + 흰 잉크는 3.71:1로 AA 미달이다(§3.3 ★★). `--color-accent`는 포커스 링·보더·아이콘 등 **비텍스트 용도**로 남는다.
- **brand 버튼의 잉크는 다크에서도 `#191f28`이다.** 노랑 표면 위 잉크는 항상 어두운 중성색(§3.4).
- hover: **배경만 위 표의 한 단계 위로** + `translateY(-1px)`까지. 그림자·스케일 금지. 전환은 `--transition-fast`(150ms — 색 변화 대역, §5.5).
- disabled: `opacity: .5`, 커서 `not-allowed`.
- focus-visible: `outline: 2px solid var(--color-accent); outline-offset: 2px` (전역 규칙 상속).
  **★ 현행 부작용:** 전역 `:focus-visible`(globals.css:212-216)에 `border-radius: 4px`가 함께 걸려 있어, `rounded-full`인 FAB·필터 칩의 포커스 링이 **각진 사각형**으로 그려진다. 전역 선언에서 `border-radius`를 빼고, 링 모양은 요소 자신의 반경을 따르게 한다(§9 P5-12).

### 6.3 폼 입력

| 속성 | 라이트 | 다크 |
|---|---|---|
| 배경 | `var(--color-bg-surface)` `#ffffff` | `#191f28` |
| 텍스트 | `var(--color-text-primary)` | `#f2f4f6` |
| 플레이스홀더 | `var(--color-text-tertiary)` | `#6b7684` — **플레이스홀더는 §3.2가 허용한 유일한 tertiary 용도다**(읽으라고 놓은 글자가 아님). 라벨을 플레이스홀더로 대체하지 않는다 |
| 보더 | `1px solid var(--color-border)` | `#2a3140` |
| 반경 | `var(--radius-md)` 8px | 동일 |
| 크기 | `px-3 py-2.5`, min-height 44px, 15px/400 | 동일 |
| focus | `outline: 2px solid var(--color-accent); outline-offset: 2px` (전역) | 동일 |

- **입력 포커스 색은 파랑이다.** 현행 `/tools`의 `focus:border-yellow-400 focus:ring-yellow-200`(`ContractCheckClient.tsx:114`)은 §3.4가 금지하는 "노랑을 포커스 링에" 위반이다. 파랑으로 되돌린다.
- 라벨: `--text-sm` / 500 / `var(--color-text-secondary)`, 입력과 6px 간격.
- 필수 표시: 라벨 뒤 `*`를 `var(--color-danger)`로. 색만으로 구분하지 않고 `aria-required`를 함께 준다.
- 다크에서 `input/select/textarea`는 배경을 명시적으로 지정한다. 지정하지 않으면 브라우저 기본 흰 배경 위에 밝은 잉크가 얹혀 읽을 수 없게 된다(사내 A 실앱 QA 사례).

### 6.4 배지

배지 텍스트는 **항상 `-ink` 토큰**을 쓴다(§3.3). 전경색(`--color-danger` 등)을 틴트 위 텍스트로 쓰면 AA에 미달한다.

| 변형 | 배경 | 텍스트 | 보더 |
|---|---|---|---|
| 중립 | `var(--grey-100)` | `var(--grey-700)` `#4e5968` **(신규)** — `--color-text-secondary` `#6b7684`는 페이지 바탕 위 4.42:1로 미달(§3.2 규율 2) | 없음 |
| 정보 | `var(--color-info-bg)` | `var(--color-info-ink)` | 없음 |
| 성공 | `var(--color-success-bg)` | `var(--color-success-ink)` | 없음 |
| 주의 | `var(--color-warn-bg)` | `var(--color-warn-ink)` | 없음 |
| 위험 | `var(--color-danger-bg)` | `var(--color-danger-ink)` | 없음 |
| 브랜드(NEW 등) | `var(--color-brand-solid)` | `#191f28` 고정 | 없음 |

공통: `--radius-sm` 6px(칩 형태는 `rounded-full`), `px-2 py-0.5`, `--text-xs` / 500, 그림자 없음.

`ContractCheckClient.tsx:29-34`의 `STATUS_STYLE` 4색은 위 위험/주의/성공/중립에 1:1로 매핑되며, **그 파일의 hex 페어 4쌍이 곧 이 표의 근거다** — `#fee2e2/#b91c1c`(5.30:1), `#fef3c7/#b45309`(4.51:1), `#dcfce7/#15803d`(4.57:1), `#e2e8f0/#475569`(6.15:1). 토큰화는 값을 바꾸는 작업이 아니라 이름을 붙이는 작업이다 — 색을 `#dc2626`·`#059669`로 "정리"하면 대비가 후퇴한다.

주의(warn) 잉크만은 예외다. 이 파일의 risk 전경은 `#b45309`(4.51:1, 경계선상 통과)이지만, **레포 전역의 지배적 주의 페어는 `#fef3c7`/`#92400e`(17곳, 6.37:1)** 이므로 `--color-warn-ink`는 후자를 택한다. `#b45309`는 `--color-warn`(솔리드·아이콘·좌측선)으로 남는다.

### 6.5 표

| 속성 | 값 |
|---|---|
| 컨테이너 | 카드 규격(§6.1) — 반경 `--radius-xl` 14px + `overflow-x: auto` |
| 헤더 | `--text-xs` / 500 / **`var(--grey-700)`** **(신규)** — 11~12px이므로 §3.2 규율 2에 따라 `--color-text-secondary`를 쓰지 않는다(§6.4 중립 배지와 동일 판정) |
| 헤더 배경 | **`var(--color-bg-elevated)`** — 라이트 `#ffffff` / 다크 `#1e2530`. 다크의 `--grey-50`은 `#191f28`이라 카드 표면과 같아져 헤더가 사라지므로 `--grey-50`을 쓰지 않는다. 라이트에서 헤더와 표면을 구분해야 하면 배경 대신 **하단 `1px solid var(--color-border)`**로 가른다 **(신규)** |
| 행 구분 | `1px solid var(--color-border-subtle)` — 참조 0회였던 토큰을 여기서 살린다 |
| 셀 패딩 | `px-4 py-3` **(신규)** |
| 행 line-height | 1.5 **(신규, 밀집 예외)** |
| 행 hover | 배경 `var(--color-bg-elevated)`. 이동·그림자 없음 |
| 숫자 열 | 우측 정렬 + `font-variant-numeric: tabular-nums` **(신규, 사내 A에서 국소 채택)** |
| 합계 행 | 상단 `1px solid var(--color-border)`, weight 600 |

`tabular-nums`는 **숫자 열이 실제로 정렬되는 표와 `HolidayPayCalculator.tsx` 계산 결과에만** 적용한다. 전역 적용은 하지 않는다(§2.2 — 그 절의 상호참조는 §6.7이 아니라 이 절이다).

### 6.6 알림 배너

| 변형 | 배경 | 좌측선 4px | 테두리(나머지 3면) | 본문 텍스트 | 제목 텍스트 | 쓰는 곳 |
|---|---|---|---|---|---|---|
| 참고 | `var(--color-info-bg)` | `var(--color-info)` | 없음 | `var(--color-text-primary)` | `var(--color-info-ink)` | **조문 인용**, 부연 설명 |
| 주의 | `var(--color-warn-bg)` | `var(--color-warn)` | `1px solid var(--color-warn-border)` | `var(--color-text-primary)` | `var(--color-warn-ink)` | 예외 조건, 사업장 규모 단서 |
| 위험 | `var(--color-danger-bg)` | `var(--color-danger)` | `1px solid var(--color-danger-border)` | `var(--color-text-primary)` | `var(--color-danger-ink)` | **요건 미충족 항목**, 법정 기한이 있는 절차의 남은 기간 |
| 성공 | `var(--color-success-bg)` | `var(--color-success)` | `1px solid var(--color-success-border)` | `var(--color-text-primary)` | `var(--color-success-ink)` | 요건 충족 |

`-border` 3종은 여기서만 쓴다 — 이 열이 없으면 §3.3이 신설한 `--color-*-border` 토큰에 용도가 없다. 참고 변형은 본문 인용에 자주 쓰여 테두리를 두면 무거워지므로 좌측선만 둔다.

공통: `border-left: 4px solid`, `--radius` 10px, `p-5`, 그림자 없음. `/guide`의 콜아웃 3종(`src/components/CalloutBox.tsx`)이 이 규격으로 수렴한다 — 현행 `tip` 변형(초록)은 **성공**에, `warning`은 **주의**에, `info`는 **참고**에 매핑한다.

**인용 블록 = 참고 배너 (신규 — 문서 내 충돌 해소).** §3.2가 `--color-accent-light`의 용도로 적은 "인용 블록 배경"과 §3.4가 파랑 허용에 적은 "인용 블록 좌측선"은 **이 표의 참고 변형과 같은 요소**를 가리킨다. 라이트에서는 둘 다 `#e8f3ff`로 우연히 같지만 다크에서 `rgba(49,130,246,.12)`(accent-light)와 `rgba(49,130,246,.16)`(info-bg)로 갈린다. **인용 블록은 참고 배너 규격을 따르며, `--color-accent-light`는 활성 네비 배경 전용으로 좁힌다.** 두 토큰의 다크 알파를 `.16`으로 맞춰도 되지만, 용도를 가르는 쪽이 우선이다.

배너 본문은 잉크 토큰이 아니라 `--color-text-primary`로 쓴다 — 긴 문장을 의미색으로 칠하면 읽기 부담이 커지고, 색은 좌측선과 제목이 이미 전달한다.

**★ 위험 변형의 문구 제약 (§8.2와의 정합).** 위 "쓰는 곳"에서 "법 위반 판정"과 "기한 임박"이라는 표현을 뺀 이유가 있다. §8.2는 "계산기와 점검 도구는 **참고 자료이지 판정이 아니다**"라고, §1은 "**경고색으로 겁을 주지 않는다**"고 정했다. 컴포넌트가 판정형·긴박형 문구를 담을 자리로 정의되면 문체 규칙을 어기지 않고도 그 톤이 나온다 — 규격이 문체보다 강하다. 따라서:

> **위험 배너는 "무엇이 빠졌는지/무엇이 다른지"를 적는 자리이지 "위법이다"라고 적는 자리가 아니다.** 남은 기간을 표시할 때도 법정 기한 자체를 사실로 적고(`제척기간은 …까지입니다`) 남은 일수를 카운트다운으로 강조하지 않는다. 문구는 §8.2 표를 따른다.

**다크에서 배너 배경은 반드시 알파 틴트다.** 라이트 파스텔(`#ecfdf5` 등)을 그대로 두면 어두운 페이지에 밝은 블록이 남고 그 위 텍스트가 뒤집혀 읽을 수 없다 — 현재 `/guide`가 렌더하는 `CalloutBox.tsx`에서 실제로 이 상태다.

**다크 알파 틴트의 검증 표면 (신규).** 알파 배경은 합성 결과가 아래 표면에 따라 달라진다. 배너가 페이지 바탕(`#0f1117`) 위에 놓일 때와 카드(`#191f28`) 안에 놓일 때 잉크 대비가 달라지므로(성공 잉크 기준 각각 약 10.4 / 9.0:1), **알파 값은 카드 안(`#191f28`)을 기준으로 검증한다** — 더 불리한 쪽이다.

### 6.7 유지되는 기존 컴포넌트

| 컴포넌트 | 규격 (라이트) | 다크 | 비고 |
|---|---|---|---|
| 스티키 glass 네비 | `.glass-nav` — blur(16px) saturate(180%), 높이 56px, `max-w-[1400px]`, 배경 `rgba(255,255,255,.72)`, 하단선 `rgba(255,255,255,.5)` | 배경 `rgba(15,17,23,.82)`, 하단선 `rgba(255,255,255,.06)` (globals.css:190-193 실재) | **Glass는 크롬에만.** 본문은 솔리드. `.glass-panel`·`.glass-elevated`는 사용처 0 → 삭제 후보 |
| 상담 FAB | `rounded-full`, `px-4 py-3`, 배경 `var(--color-accent-ink)` `#1b64da`, 잉크 흰색(5.41:1) | 배경 `var(--color-accent)` `#4593fc`, 잉크 `#0f1117` | 그림자를 `var(--shadow-lg)`로 교체(§5.4). 배경은 §3.3 ★★에 따라 accent → accent-ink. 배치 제약은 아래 ★ |
| 필터 칩 | `rounded-full`, `px-3 py-1.5`, `--text-xs`, 보더 `1px solid var(--color-border)`, 배경 `var(--color-bg-surface)`, 잉크 `var(--grey-700)` | 토큰이 자동 대응 | 선택 시 배경 `var(--color-accent-ink)` + 흰 잉크(라이트) / `var(--color-accent)` + `#0f1117`(다크). **시각 높이 30px + 투명 히트 영역 44px**(§5.1 예외표) |
| 아코디언(FAQ) | 카드 규격 + 트리거 `button` 전폭, 트리거 `min-height: 44px` | 토큰이 자동 대응 | 펼침 전환 `--transition-normal`(300ms, 크기 변화 대역) |
| `decisions-ui/*` | shadcn 유틸 레이어. `/sanction`·`/search`·`/stats`·`/decisions`·`/harassment`에서만 | — | §9 P1의 죽은 유틸 2건을 고쳐야 정상 렌더된다. `--destructive`가 이 레이어에서만 살아 있다(§3.5) |

**★ 상담 FAB의 배치 제약 (신규).** FAB는 `layout.tsx:154`에서 전역 마운트되어 `/ai`를 뺀 모든 화면 우하단에 고정 부유하는 **상업적 유도 요소**다. §1은 "인터페이스는 문서를 방해하지 않는 만큼만 존재한다"고 정했으므로, 문구(§8)만이 아니라 배치도 규율한다:

> 1. 본문 스크롤 영역을 가리지 않는다 — 모바일에서 본문 마지막 줄과 겹치면 하단 여백을 확보한다.
> 2. **법령 본문·판정 결과·계산 결과와 시각적으로 인접시키지 않는다.** 근거를 읽는 자리와 상담을 파는 자리는 붙이지 않는다.
> 3. 진입 애니메이션·주기적 흔들림·자동 확장 금지(§1 "애니메이션으로 시선을 끌지 않는다").
> 4. 면책·출처 표기가 있는 블록 위에 겹치지 않는다.

### 6.8 읽는 화면의 조판 (신규)

§1이 이 사이트의 정체성으로 삼은 것은 **긴 한국어 산문**인데, §6.1~6.7에는 그것을 렌더하는 규격이 없었다. `DESIGN_GUIDE.md`가 콘텐츠 블록 5종을 규정했다가 폐기되면서(§0.2) 대체물이 없는 상태이므로 여기서 최소 규격을 정한다.

| 요소 | 규격 |
|---|---|
| 산문 컨테이너 | `.content-body` — `--text-base` / 400 / LH 1.65 / LS -0.01em / `max-width: 38em`, 컨테이너는 `max-w-[820px]` 좌측 정렬(§4.3) |
| 문단 간격 | 문단 사이 `1.25em`(margin-bottom). 들여쓰기는 쓰지 않는다 |
| **본문 링크** | 색 `var(--color-accent-ink)`(라이트 `#1b64da` 5.41:1 / 다크 `#4593fc` 6.14:1) + **상시 `text-decoration: underline`**, `text-underline-offset: 0.2em`, `text-decoration-thickness: 1px`. hover에서 `var(--color-accent-ink-hover)`로 색만 바꾼다 — **`--color-accent-hover`가 아니다**(라이트에서 `--color-accent-ink`와 값이 같아 hover가 무효가 된다, §3.3) |
| 링크 밑줄이 필수인 이유 | 현행은 `hover:underline`뿐이라 평상시 링크가 **색만으로** 구분된다. 그 색이 페이지 바탕 위 3.55:1이므로 WCAG **1.4.1(색 단독 사용)**과 **1.4.3(대비)**을 동시에 어긴다. 검색으로 도착한 비전문가가 "여기를 누르면 다음 문서로 간다"(§1)를 인지하지 못한다 |
| 밑줄 예외 | 네비·버튼·카드 전체가 링크인 경우·목록 항목 제목처럼 **블록 자체가 클릭 대상임이 형태로 명백한 곳**은 밑줄 없이 색만. 산문 안에 섞인 인라인 링크는 예외 없이 밑줄 |
| `visited` | 별도 색을 주지 않는다(법령 문서에서 방문 이력은 의미가 없고, 색을 하나 더 늘리면 §3.4 2액센트 체계가 깨진다) |
| 목록 `ul`/`ol` | `padding-left: 1.4em`, 항목 간 `0.4em`, 마커는 `--color-text-secondary`. 중첩은 2단까지 |
| `h5`·`h6` | 만들지 않는다. 4단계로 안 되면 문서를 쪼갠다(§4.2가 h1~h4만 규정한 것은 누락이 아니라 상한이다) |
| 조문 인용 | §6.6 **참고 배너** 규격. 인용 안의 조문 번호는 weight 600, 본문은 `--color-text-primary` |
| 강조 | `<strong>` = weight 600, 색 변경 없음. 형광펜·배경색 강조 금지 |
| 코드·라틴 라벨 | mono, `--text-sm`. **한글을 mono로 렌더하지 않는다**(§2.1) |

---

## 7. 다크모드

### 7.1 현재 상태 진단

다크가 **두 축으로 갈라져 서로를 모른다.**

| 축 | 기전 | 상태 |
|---|---|---|
| CSS 변수 | `ThemeToggle.tsx:14,20`이 `documentElement.classList.toggle('dark')` → `.dark {}` 블록의 토큰 재정의 | **작동한다** (하이드레이션 이후) |
| Tailwind `dark:` 유틸 78개 | globals.css에 `@custom-variant dark` 선언이 없어 Tailwind v4 기본대로 `@media (prefers-color-scheme: dark)`로 컴파일 | **OS 설정에만 반응.** 토글과 무관 |

결과:

- OS 라이트 + 토글로 다크 켬 → 변수만 반전, `dark:` 유틸 78개는 라이트 그대로 (반쪽 반전)
- OS 다크 + 토글로 라이트 켬 → 변수는 라이트, `dark:` 유틸은 계속 다크 (정반대 파손)
- SSR HTML에는 `.dark`가 없고 `layout.tsx`에 pre-hydration 스크립트도 없다 → 다크 사용자에게 **흰 화면 플래시(FOUC)**

### 7.2 규칙

1. **다크의 유일한 활성화 축은 `.dark` 클래스다.** globals.css에 `@custom-variant dark (&:is(.dark *));`를 선언해 `dark:` 유틸을 같은 축에 합류시킨다. (§9 P0)
2. **우선순위:** `localStorage.theme` > `prefers-color-scheme`. `ThemeToggle.tsx`가 이미 이 순서로 구현돼 있다.
3. **FOUC 차단:** `layout.tsx` `<head>`에 pre-hydration 인라인 스크립트로 `.dark`를 선반영한다. (§9 P0)
4. **라이트가 정본이다.** 비로그인 SEO 방문자는 라이트로 도착한다. 다크는 정직한 보조 — 라이트에 있는 화면은 다크에서도 전부 읽혀야 하지만, 다크를 위해 라이트를 바꾸지 않는다.
5. **다크 대응의 유일한 방법은 토큰이다.** 색을 인라인 `var(--color-*)`로 넣으면 다크가 자동으로 따라온다. 생 hex와 Tailwind 팔레트 유틸(`text-slate-700`, `bg-slate-50`)은 다크에서 반전되지 않는다. 실측(2026-08-01): **6자리 생 hex — `src/**/*.{ts,tsx}` 284개 + `globals.css` 47개 = 331개**(3자리 hex 41개 별도), **Tailwind 팔레트 유틸 — `src/**/*.tsx` 203회**(`(text|bg|border|ring|from|to|via|divide|…)-(slate|gray|red|…)-(50|N00|950)` 기준). 이 전부가 잠재 파손이다.
6. **연한 배경 + 어두운 잉크 조합은 다크에서 반드시 알파 틴트로 바꾼다.** 파스텔 배경을 그대로 두면 어두운 페이지 위에 밝은 블록이 남고 그 위 텍스트가 반전되어 읽을 수 없다.
7. **어두운 배경을 하드코딩한 블록을 만들지 않는다.** `HomeClient.tsx:367-378`의 CTA 밴드(`#191f28` 배경)는 다크 페이지 배경(`#0f1117`)과 거의 같은 톤이라 블록 경계가 사라진다. 이런 "다크 밴드" 연출은 `var(--grey-900)`을 쓰거나 아예 카드로 바꾼다.
8. `<meta name="theme-color">`는 라이트/다크 두 값을 `media` 속성으로 나눠 준다. 현행 `#1d4ed8`(`layout.tsx:65`)은 팔레트 어디에도 없는 제3의 파랑이다.

### 7.3 수정 후 필수 점검

**다크에서 확인할 화면:** `/` 하단 CTA 밴드(`HomeClient.tsx:367-378`) · `/` 기능 카드 색 페어 **8쌍**(`HomeClient.tsx:65-125`) · `/guide` 콜아웃 3종(`CalloutBox.tsx`)과 `#059669` 계열 · `/tools` 전 화면(slate 팔레트) · `/tools/contract-check` 판정 배지 4색 · `HolidayPayCalculator.tsx` 결과 패널 · `/sanction`(죽은 유틸 영향) · `lib/category-colors.ts` 5개 카테고리 칩.

**라이트에서 확인할 것 (신규 — 라이트가 정본인데 체크리스트가 없었다):** 아래는 눈으로 보는 점검이 아니라 **측정**이다.

| 항목 | 기준 | 방법 |
|---|---|---|
| 본문 링크 대비 | ≥ 4.5:1 (페이지 바탕 `#f9fafb` 기준) | `--color-accent-ink` 적용 후 재측정 |
| 링크 식별 | 색 외 단서 존재 | 밑줄 렌더 확인(§6.8) |
| 솔리드 배경 + 잉크 | ≥ 4.5:1 | 버튼 4변형·FAB·선택 칩 전수 |
| 틴트 배경 + 잉크 | ≥ 4.5:1 | 배지 6변형·배너 4변형 전수 |
| 보조 텍스트 | ≥ 4.5:1, 표면별 | `--color-text-secondary`가 `#f9fafb` 위에 놓인 곳 색출 |
| 캡션·날짜 | tertiary 미사용 | `--color-text-tertiary`가 콘텐츠에 쓰인 곳 색출(§3.2) |
| 최소 글자 크기 | ≥ 11px | `text-[10px]` 등 임의값 색출(§4.3) |
| 터치 타깃 | ≥ 44×44px 히트 영역 | 칩·ghost 버튼·네비 링크 |

이 점검은 §9 P6-4로 마이그레이션 계획에 편입한다 — §3.3이 AA를 근거로 토큰 17종(의미색 15 + `--color-accent-ink` + `--color-accent-ink-hover`)을 만들었는데 그것이 지켜졌는지 확인하는 단계가 없으면 근거가 무의미하다.

---

## 8. 마이크로카피

### 8.1 기본 레지스터

- **합니다체로 통일한다.** 해요체(`~해요`, `~할게요`)·음슴체 금지. 현행이 이미 합니다체다.
- 한 문장에 한 가지만 담는다. 두 가지가 되면 문장을 나눈다.
- 마침표를 찍는다. 느낌표·물결표(`~`)·이모지는 제품 크롬에 쓰지 않는다.
- 구현 어휘를 화면에 노출하지 않는다: `fallback`, `mock`, `API`, `파싱`, `쿼리`, `null`.
- 마케팅 최상급 금지: `최고의`, `완벽한`, `혁신적인`, `단 한 번의 클릭으로`.

### 8.2 법률 정보 사이트 고유 규칙 — 단정 금지 + 확인 경로

이 사이트의 문장은 사람의 법적 판단에 영향을 준다. 계산기와 점검 도구는 **참고 자료이지 판정이 아니다.**

| 규칙 | 나쁨 | 좋음 |
|---|---|---|
| 단정 대신 조건 명시 | `가산수당을 받을 수 있습니다.` | `5인 이상 사업장이라면 가산수당 대상입니다. 5인 미만은 의무가 없습니다.` |
| 결론에 확인 경로를 붙인다 | `이 계약서는 위법입니다.` | `근로기준법 제17조 필수 명시사항이 빠져 있습니다. 사업장에 수정 요청을 하거나 관할 노동청에 문의하시기 바랍니다.` |
| 인증·보증 어휘 금지 | `법적으로 검증된 계산 결과` | `공개된 법령·행정해석에 따라 계산한 결과` |
| 도구의 한계를 먼저 말한다 | (없음) | `실제 판단은 근로계약서 전문과 사업장 사정에 따라 달라집니다.` |

**★ 데이터 처리에 관한 문장은 구현과 대조한 뒤에 쓴다 (신규 규율).** "저장되지 않습니다", "서버로 보내지 않습니다", "브라우저를 떠나지 않습니다"는 문체가 아니라 **사실 주장**이고, 개인정보 관련 진술이라 틀리면 문체 오류가 아니라 허위 고지가 된다. 이 사이트의 도구는 근로계약서 사진(성명·임금·때로 주민등록번호)을 다루므로 노출 지점이 가장 직접적이다.

> 1. **미저장(not stored)과 미전송(not transmitted)은 다른 주장이다.** 둘을 바꿔 쓰지 않는다. 문체를 다듬다가 주장의 범위가 넓어지지 않았는지 확인한다.
> 2. 데이터 문구를 쓰거나 고칠 때는 **해당 기능의 네트워크 호출을 직접 확인한다.** 예: `PhotoUploadCard.tsx:107`은 `/api/tools/contract-check/extract`로 이미지를 POST하므로, 그 화면에서 "전송하지 않습니다"라고 쓸 수 없다.
> 3. 전송이 있으면 **전송 사실과 보존 여부를 함께** 적는다(§8.3 인용 예시가 이 형태다).
> 4. 이 문구들은 개인정보처리방침과 같은 사실을 말해야 한다. 어긋나면 문구가 아니라 둘 중 하나가 틀린 것이다.

현행 코드에 이 레지스터의 좋은 실물 예시가 있다. 새 문장은 이 톤에 맞춘다:

> `근거 법령이 다릅니다. 관공서 공휴일(빨간날)은 근로기준법 제55조·시행령 제30조 — 5인 이상 사업장만 유급휴일입니다. 노동절(5/1)은 「노동절 제정에 관한 법률」 — 5인 미만 사업장 포함 모든 사업장 유급휴일입니다.`
> — `src/app/tools/holiday-pay` FAQ 데이터

### 8.3 번역투 제거

한국어 UI에서 자주 나오는 영어 직역 패턴을 걷어낸다. 아래 "나쁨"은 **일반 예시이며 이 레포에서 인용한 문장이 아니다.**

| 패턴 | 나쁨 (예시, 레포 인용 아님) | 좋음 |
|---|---|---|
| 주어 없는 수동태 | `근로시간이 입력되면 결과가 표시됩니다.` | `근로시간을 입력하시면 결과를 보여드립니다.` |
| 사물 주어 | `이 도구는 당신을 도와줍니다.` | `이 도구로 필수 항목을 점검할 수 있습니다.` |
| `~하는 것이 가능합니다` | `계산하는 것이 가능합니다.` | `계산할 수 있습니다.` |
| `~에 대해` 남용 | `해고에 대해 알아보기` | `해고 요건 보기` |
| `귀하` | `귀하의 근로시간을 입력하세요` | `근로시간을 입력해 주세요` |
| 영어식 시제 | `저장되었습니다` (완료 강조 불필요할 때) | `저장했습니다` |
| 명령형 | `클릭하세요` | `눌러 주세요` / `선택해 주세요` |

레포 현행 문장 중 이 규칙을 이미 충족하는 예 **(전문 인용 — 잘라 쓰면 주장이 달라진다)**:

> `입력하신 내용은 저장되지 않습니다. 이 화면에서 계산에만 쓰이고, 창을 닫으면 사라집니다.`
> `사진은 글자를 읽어내는 동안에만 서버를 거치고, 사진과 읽어낸 결과 모두 남기지 않습니다.`
> — `src/app/tools/contract-check/ContractCheckClient.tsx:316-321`

앞 문장만 떼어 인용하면 사진의 서버 전송(`PhotoUploadCard.tsx:107`)을 감추는 문장이 된다. **뒤 문장이 그 사실을 밝히기 때문에 이 문구가 정본 예시로 성립한다** — §8.2 ★규율 3의 실물이다.

### 8.4 상태 문구 표준 (신규)

| 상황 | 문구 |
|---|---|
| 빈 상태 | `아직 결과가 없습니다. 조건을 입력하고 계산을 눌러 주세요.` |
| 진행 중 | `계산 중입니다.` (`...` 대신 마침표) |
| 오류 | `계산에 실패했습니다. 잠시 후 다시 시도해 주세요.` |
| 검색 결과 없음 | `검색어와 일치하는 문서가 없습니다. 다른 낱말로 찾아보시기 바랍니다.` |
| 면책 | `참고용 계산 결과입니다. 실제 지급액은 근로계약과 사업장 사정에 따라 달라집니다.` |

**상태의 시각 규격 (신규).**

| 상황 | 시각 |
|---|---|
| 빈 상태 | 카드 규격 안에 중앙 정렬, 문구 `--text-sm` / `var(--grey-700)`, 아이콘 24px `var(--color-text-tertiary)`. 일러스트 없음 |
| 진행 중 | **스켈레톤 우선** — 결과 블록과 같은 크기의 `var(--grey-100)` 사각형, `--radius-md`. 애니메이션은 opacity 0.6↔1, 1.5s, `prefers-reduced-motion`에서 정지. 스피너는 위치를 예측할 수 없는 경우에만 |
| 오류 | §6.6 **주의** 배너(위험 아님 — 도구 실패는 법적 위험이 아니다) |
| 검색 결과 없음 | 빈 상태와 동일 |
| 면책 | 배너가 아니라 결과 블록 **하단 캡션**. `--text-sm` / `var(--grey-700)`. 배너로 만들면 결과보다 면책이 커진다 |

### 8.5 라벨 규칙

- 버튼 라벨은 **동사구**로: `계산하기`, `계약서 점검하기`, `상담 신청`. `확인`·`제출` 같은 무맥락 라벨 금지.
- 네비·메뉴는 **명사구**로: `노동법 데이터베이스`, `판정례 검색`, `자주 묻는 질문`.
- 법령명은 정식 명칭 + 낫표: `「근로기준법」 제56조`. 약칭 사용 시 첫 등장에서 한 번 풀어 쓴다.
- 숫자에 단위를 붙일 때 숫자와 단위 사이를 띄우지 않는다: `13,000원`, `40시간`, `5인`.

---

## 9. 마이그레이션 — 무엇부터 고치는가

이 절은 **명세**다. 실행은 별도 작업으로 한다. §0.1에 따라 여기서 지시하는 globals.css 변경은 "코드가 정본"에 막히지 않는다 — 규범을 코드에 반영하는 작업이다.

**순서의 근거.** P0는 "다른 수정을 무의미하게 만드는 전역 파손"이고, P0-2·P0-3이 다크 배관인 것은 다크를 라이트보다 우선해서가 아니라 **`dark:` 유틸 78개가 토글과 다른 축에 붙어 있는 한 이후의 모든 색 작업이 두 번 검증돼야 하기 때문**이다(§7.1). 라이트 정본성(§7.4)과 충돌하지 않는다.

### P0 — 전역 파손 (다른 모든 수정의 전제)

| # | 파일 | 조치 |
|---|---|---|
| P0-1 | `src/app/globals.css:14` | `--font-sans` 스택 선두의 `var(--font-pretendard), ` 삭제(정의 0건 — 확인됨). **수정 전후로 실제 브라우저에서 `getComputedStyle(document.body).fontFamily`를 확인해 기록한다** — "폴백으로 떨어진다"는 결과 서술은 아직 런타임으로 검증되지 않았다(§4.1) |
| P0-2 | `src/app/globals.css` (상단) | `@custom-variant dark (&:is(.dark *));` 선언 추가. `dark:` 유틸 78개(15개 파일)를 `.dark` 클래스 축에 합류시킨다 |
| P0-3 | `src/app/layout.tsx` `<head>` | pre-hydration 인라인 스크립트 추가 — `localStorage.theme` > `prefers-color-scheme` 순으로 `.dark`를 SSR 직후 선반영. 다크 사용자 FOUC 제거 |

### P1 — 죽은 유틸 (조용히 스타일이 빠지는 지점)

| # | 파일 | 조치 |
|---|---|---|
| P1-1 | `src/app/sanction/page.tsx:209,270,292,318,332,471` | `--color-accent-shadcn`(globals.css:27) 때문에 `@theme`에 `accent` 색 키가 없어 **`bg-accent` 유틸이 생성되지 않는다**(배경이 아예 안 칠해짐). **결정: 이름은 그대로 두고 호출부를 옮긴다.** `@theme`에 `--color-accent: var(--accent)`를 추가하면 `:root`의 브랜드 `--color-accent`(#3182f6)를 shadcn의 blue-50으로 덮어써 **인라인 `var(--color-accent)` 215곳이 전부 창백해진다** — 브랜드 이름이 우선한다. 따라서 `bg-accent` → `bg-muted`(= `--secondary`/`--muted` = `--grey-100`, shadcn `accent`의 "muted hover 표면" 의미와 일치, `@theme:25`에서 이미 생성됨), `hover:bg-accent/60` → `hover:bg-muted/60`로 교체. `text-accent-foreground`(8회)는 정상 생성되므로 그대로 둔다 |
| P1-1b | `src/components/ui/button.tsx:16,20` | 위와 동일 증상이나 이 파일은 임포트 0회 → P5-7에서 파일째 삭제하므로 별도 수정 불요 |
| P1-2 | `src/app/globals.css:11-42` | `@theme inline`에 `--color-border`가 없어 **`border-border` 유틸이 생성되지 않는다**(border-color가 currentColor로 떨어짐). 영향처: `src/components/decisions-ui/badge.tsx:18`, `src/components/decisions-ui/button.tsx:15`, `src/app/sanction/page.tsx:381,390,433,440,446,502`. `border-border/50` 같은 알파 변형도 같은 원인으로 죽는다 |

### P2 — 노랑 토큰화 (§3.4)

| # | 파일 | 조치 |
|---|---|---|
| P2-1 | `src/app/globals.css` | `--brand-50 ~ 800` 램프 + `--color-brand-solid/surface/border/ink` 역할 토큰 신설. 기존 `--yellow-100` → `--brand-100`으로 흡수, 다크값 `#422006` 폐기. **§6의 brand 규격은 이 작업 이후에만 구현 가능하다** |
| P2-2 | `src/app/tools/page.tsx:71,74,85-87,95` + hex `79,92,115` | `border-slate-200 hover:border-yellow-400` → 토큰, `bg-yellow-100 text-yellow-700` → `--color-brand-surface` / `--color-brand-ink`, `#0f172a`·`#475569`·`#94a3b8` → 텍스트 토큰 |
| P2-3 | `src/app/tools/contract-check/ContractCheckClient.tsx` | `:55(#facc15)`, `:114(focus:border-yellow-400 focus:ring-yellow-200 → 파랑 포커스로 되돌림)`, `:185,215`, `:189(#854d0e)`, `:223`, `:514,574`, `:760,800,809(bg-yellow-400)` 토큰화 |
| P2-4 | `src/components/HolidayPayCalculator.tsx` | 선택 카드의 생 hex → 토큰: `:441-442`, `:478-479`, `:519-520`(`#facc15` → `--color-brand-border`, `rgba(250,204,21,0.12)` → `--color-brand-surface`), `:559`·`:612`(`rgba(250,204,21,0.10)` → `--color-brand-surface`), `:36`·`:346`(`#facc15`), `:662-663`(`border-yellow-400` + `#92400e`). **`src/app/tools/holiday-pay/`에는 hex가 사실상 없다**(`page.tsx:47`의 `#94a3b8` 1건) — 도구 UI는 전부 이 컴포넌트에 있다 |

### P3 — 의미색 토큰 신설 (§3.3)

| # | 파일 | 조치 |
|---|---|---|
| P3-1 | `src/app/globals.css` | `--color-success/warn/danger/info` + `-ink` + `-bg` + `-border` 15종 + `--color-accent-ink` + `--color-accent-ink-hover` 신설, `.dark` 알파 틴트 대응 (§3.3). **§6.4 배지 5변형·§6.6 배너 4변형 전부가 이 작업에 선행 의존한다** |
| P3-2 | `src/components/CalloutBox.tsx` **(주 대상)** | 콜아웃 3종의 하드코딩 hex → 의미색 토큰. `:5` warning(`#fef3c7`/`#fde68a`/`#d97706`/`#92400e`) → warn 4종, `:6` tip(`#f0fdf4`/`#bbf7d0`/`#16a34a`/`#166534`) → success 4종. `:4` info는 이미 `var(--blue-*)` 사용 중 → `--color-info-*`로 개명. 이후 §6.6 배너 규격으로 수렴 |
| P3-2b | `src/app/guide/page.tsx` | 자체 hex 6개(`#059669`×3, `#ecfdf5`, `#166534`, `#ef4444`) → 토큰 |
| P3-2c | 나머지 의미색 하드코딩 파일 | `#fef3c7`/`#92400e` 페어를 쓰는 `ChecklistWidget.tsx:72,165`·`DeepChecklistWidget.tsx:27`·`SimpleChecklistWidget.tsx:54,76`·`StepDiagram.tsx:9,78`·`news/NewsClient.tsx:372`·`cases/[id]/page.tsx:122`·`admin/AdminClient.tsx:61`·`blog/BlogClient.tsx:286`·`PhotoUploadCard.tsx:203`·`ContractCheckClient.tsx:98` → warn 토큰 |
| P3-3 | `src/app/tools/contract-check/ContractCheckClient.tsx:29-34` | `STATUS_STYLE` hex 8개 → 배지 토큰 4변형으로 **이름만** 교체. 이 페어들은 이미 AA를 통과하므로 **값을 바꾸지 않는다** — `#b91c1c`→`--color-danger-ink`, `#15803d`→`--color-success-ink`, `#475569`→중립 잉크. `#dc2626`·`#059669`로 "정리"하면 대비가 후퇴한다(§6.4) |
| P3-4 | `src/lib/category-colors.ts:1-9` (hex는 `:2-6`) | 5개 카테고리 hex 페어 → 토큰. `:7-8`(종합·general)은 이미 `var(--grey-*)` 사용 중. 현재 다크에서 라이트 파스텔이 그대로 남는다 |

### P4 — 다크에서 실제로 깨지는 화면

| # | 파일 | 조치 |
|---|---|---|
| P4-1 | `src/app/HomeClient.tsx:367-378` | 하단 CTA 밴드 하드코딩(`#191f28` / `#f2f4f6` / `rgba(242,244,246,0.7)`) → 토큰. 다크에서 페이지 배경과 톤이 겹쳐 블록 경계가 사라진다 |
| P4-2 | `src/app/HomeClient.tsx:65-125` | 기능 카드 색 페어 **8쌍** → 다크 알파 틴트 대응. 전량: `:65/66` `#8b5cf6`/`#f5f3ff` · `:73/74` `#dc2626`/`#fef2f2` · `:81/82` `#059669`/`#ecfdf5` · `:89/90` `#7c3aed`/`#f5f3ff` · `:100/101` `#0ea5e9`/`#f0f9ff` · `:108/109` `#0284c7`/`#e0f2fe` · `:116/117` `#16a34a`/`#f0fdf4` · `:124/125` `#ca8a04`/`#fefce8`. **범위를 `:65-124`로 잡으면 마지막 페어의 `bg`(`:125`)가 잘린다** |
| P4-3 | `src/app/tools/**` **+ `src/components/HolidayPayCalculator.tsx`** | slate 팔레트 유틸(`border-slate-200`, `bg-slate-50`, `text-slate-700` 등)을 토큰으로. `slate-200`(#e2e8f0)은 `--color-border`(#e5e8eb)와 실색이 달라 경계선 톤이 페이지마다 어긋난다. **`src/app/tools/**` glob만으로는 부족하다** — `/tools/holiday-pay`의 slate·`border-2`는 전부 `HolidayPayCalculator.tsx`에 있고 이 파일은 `src/components/` 아래다 |
| P4-4 | `src/app/globals.css:240-249` | 라이트 `body`의 3중 오로라 `radial-gradient` + `background-attachment: fixed` 제거 → 평면 `var(--color-bg-primary)`. §1·§3.2가 전제하는 바탕과 어긋나고, `fixed`는 모바일 스크롤 리페인트 비용이다. `.dark body`(183-188)는 별건으로 남긴다 |

### P5 — 일관성 정리

| # | 대상 | 조치 |
|---|---|---|
| P5-1 | 전 페이지 | 콘텐츠 컨테이너 최대폭 **25종 → 3종**(1400 / 1100 / 820)으로 통일, 화면별 배정은 §5.2 표 |
| P5-2 | 전 페이지 | 카드 반경 `lg/xl/2xl` 혼재 → `rounded-xl` 단일. 카드 `border-2` 8회 → 1px (§5.3). 스피너(`ai/page.tsx:10`)·타임라인 점(`guide/page.tsx:138`)의 `border-2`는 **대상 아님** |
| P5-3 | 전 페이지 | 주 버튼 4규격 → §6.2의 4변형으로 통일. **primary 배경은 `--color-accent`가 아니라 `--color-accent-ink`**(§3.3 ★★) |
| P5-4 | 전 페이지 | h1 크기 지정 3방식·제목 색 4종 → §4.3 규율로 통일. `text-[10px]` 등 스케일 밖 임의값 제거 |
| P5-5 | `src/components/FloatingChatButton.tsx:20` + `.feature-card:hover` | 하드코딩 그림자 제거, `--shadow-lg` / `--shadow-md`로 교체 (§5.4). **FAB 수정 지점은 1곳**(전역 마운트 `layout.tsx:154`) |
| P5-6 | `src/app/globals.css:412-545` | `.nav-cta`·`.nav-link`의 `border-radius: 8px` → `var(--radius-md)`, `.nav-dropdown` 14px → `var(--radius-xl)` |
| P5-7 | `src/components/ui/*` (5개) | 삭제. 임포트 0회, `decisions-ui`와 중복 |
| P5-8 | `src/app/globals.css:5-9` | 참조 없는 `@font-face "Pretendard Variable Fallback"` 삭제 |
| P5-9 | `src/app/globals.css` | 죽은 토큰 정리: `--transition-slow`(참조 0), `--shadow-lg` 미사용 해소, `.glass-panel`·`.glass-elevated`·`.content-block`(사용처 0) |
| P5-10 | `src/app/globals.css` | §4.2 타이포 전달 클래스 `.t-hero`/`.t-h2`/`.t-h3`/`.t-h4`/`.t-body`/`.t-sm`/`.t-xs` 신설 — 크기·weight·LH·LS를 한 묶음으로. 현재 §4.2가 규정한 값을 담을 그릇이 없다 |
| P5-11 | `src/app/globals.css` `body` / `.blog-content` | `word-break: keep-all` + `overflow-wrap: break-word`를 `body`로 승격(현재 `.content-body`·`.blog-content`에만). `.blog-content`에 `max-width: 38em` 부여, `font-size: 15px` → `var(--text-base)`, `line-height: 1.75` → `1.65` (§4.3) |
| P5-12 | `src/app/globals.css:212-216` | 전역 `:focus-visible`의 `border-radius: 4px` 제거 — `rounded-full` FAB·필터 칩의 포커스 링이 각진 사각으로 그려진다 (§6.2) |
| P5-13 | 본문 링크 전반 | §6.8 조판 규격 적용: 링크 색 `--color-accent-ink` + 상시 밑줄, 문단·목록 간격, 조문 인용 → §6.6 참고 배너 |

### P6 — 마감

| # | 파일 | 조치 |
|---|---|---|
| P6-1 | `src/app/layout.tsx:65` | `<meta name="theme-color" content="#1d4ed8">` → 라이트 `#3182f6`(또는 `#ffffff`) / 다크 `#0f1117`를 `media` 속성으로 분리. 현재 값은 팔레트에 없는 제3의 파랑이다 |
| P6-2 | `src/app/globals.css:125-129` | `--chart-1~5` 단색 블루 5단계 → 범주형 대비가 있는 팔레트로 재지정(별건 과제) |
| P6-3 | `DESIGN_GUIDE.md` | 이 문서로 대체되었음을 최상단에 명시하거나 삭제 |
| P6-4 | 전 화면 | **명도대비 검증 통과** — §7.3 라이트 점검표 8항목 전수. §3.3이 AA를 근거로 토큰 17종(의미색 15 + `--color-accent-ink` + `--color-accent-ink-hover`)을 만들었으므로, 이 단계 없이 P2·P3을 완료로 선언하지 않는다 |
| P6-5 | 데이터 처리 문구 전수 | §8.2 ★규율에 따라 "저장/전송하지 않습니다" 계열 문장을 실제 네트워크 호출과 대조. 개인정보처리방침과의 정합도 함께 확인 |

---

## 10. 이 문서가 아직 정하지 않은 것 (미결)

규격이 없는 곳을 "없다"고 적어 두는 것이 침묵보다 낫다. 아래는 **화면을 만들려면 필요한데 이 문서가 값을 주지 않은 항목**이며, 구현자가 임의로 정하지 말고 결정을 요청해야 하는 지점이다.

| # | 항목 | 상태 |
|---|---|---|
| 10-1 | **신뢰성 표기 컴포넌트** — 최종 갱신일 / 법령 개정 반영 시점, 근거 조문 출처 링크, 작성·감수 노무사 표기, 자료 범위와 한계 고지의 배치 | §1이 성공 기준으로 내건 "법적으로 신뢰할 만해 보이는가"를 담당하는 컴포넌트가 §6에 하나도 없다. **별건 과제로 최우선.** 현행 면책은 §8.4 문구 1종이 계산기 결과에만 걸려 있다 |
| 10-2 | **브랜드 노랑 ↔ 주의 앰버의 근본 분리** | §3.4가 형태(좌측선 유무)로 임시 분리했으나 색상 자체의 명도차는 1.04:1이다. 브랜드를 앰버에서 떨어뜨리거나 주의를 오렌지 대역으로 옮기는 결정이 필요 |
| 10-3 | **브레이크포인트 값과 그리드 열 수** | §5.1이 `sm:`·`md:` 표기를 쓰면서 실제 픽셀값과 각 단의 열 수를 한 번도 명시하지 않았다. 1100px 그리드 화면을 카드 간격 16px만으로는 만들 수 없다 |
| 10-4 | **아이콘 규격** | 크기·스트로크 굵기·라이브러리(현행 `lucide-react`)가 규정되지 않았다. §5.1의 44px 판정과 §6.2 ghost 버튼 크기가 여기에 의존한다 |
| 10-5 | **부양 표면 컴포넌트** — 모달 / 드롭다운 / 팝오버 | `--color-bg-elevated`와 `--shadow-xl`에 용도를 배정해 놓고 규격이 §6에 없다. 라이트에서 elevated = `#ffffff`로 surface와 같으므로 그림자·보더 외 구분 수단도 정해야 한다 |
| 10-6 | **범주형 차트 팔레트** | §3.5·P6-2 — `--chart-1~5`가 단색 블루 5단계라 인접 계열이 구분되지 않는다. `/stats`가 recharts 기본색을 쓰는 것이 증거 |
| 10-7 | **노랑을 브랜드 정체성으로 확정하는 것의 포지셔닝 판단** | §3.4는 "제거할 수 없다"(사이트명·구현 현황)는 **매몰비용 근거만** 제시했다. 노란봉투법이 진행 중인 쟁점 사안이라는 점이 중립적 정보 제공자 지위에 주는 신호는 별도 판단이 필요하다. 같은 논법이 §2.2의 쿨 그레이 존치에도 쓰였다 — 그쪽은 색 온도라 정치적 함의가 없어 무해하다 |
| 10-8 | **다크 `body` 오로라 그라디언트 존치 여부** | P4-4가 라이트만 처리한다. 다크는 알파 0.08 이하라 눈에 띄지 않으나 `background-attachment: fixed` 비용은 동일하다 |

---

## 부록 A. 이 문서에서 새로 정한 값 일람

### A.1 신규 색 토큰

| 토큰 | 라이트 | 다크 |
|---|---|---|
| `--color-accent` (다크값만 신규) | (기존 `#3182f6`) | `#4593fc` |
| `--color-accent-ink` **(신규 토큰)** | `#1b64da` (= blue-600, 기존 램프값) | `#4593fc` (= blue-400, 기존 램프값) |
| `--color-accent-ink-hover` **(신규 토큰)** | `#1957c2` (= blue-700, 기존 램프값) | `#64a8ff` (= blue-300, 기존 램프값) |
| `--color-accent-hover` (다크값만 신규) | (기존 `#1b64da`) | `#64a8ff` |
| `--color-success` / `-ink` / `-bg` / `-border` | `#059669` / `#15803d` / `#dcfce7` / `#a7f3d0` | `#34d399` / `#6ee7b7` / `rgba(5,150,105,.16)` / `rgba(52,211,153,.32)` |
| `--color-warn` / `-ink` / `-bg` / `-border` | `#b45309` / `#92400e` / `#fef3c7` / `#fde68a` | `#fbbf24` / `#fcd34d` / `rgba(180,83,9,.18)` / `rgba(251,191,36,.32)` |
| `--color-danger` / `-ink` / `-bg` / `-border` | `#dc2626` / `#b91c1c` / `#fee2e2` / `#fecaca` | `#f87171` / `#fca5a5` / `rgba(220,38,38,.18)` / `rgba(248,113,113,.32)` |
| `--color-info` / `-ink` / `-bg` | `#1b64da` / `#1957c2` / `#e8f3ff` | `#64a8ff` / `#90c2ff` / `rgba(49,130,246,.16)` |
| 중립 배지 잉크 | `--grey-700` `#4e5968` 사용 지정 | 다크는 램프 반전으로 자동 대응 |
| `--brand-50~800` | `#fefce8` `#fef9c3` `#fef08a` `#fde047` `#facc15` `#eab308` `#ca8a04` `#a16207` `#854d0e` | 램프는 동일 |
| `--color-brand-solid` | `#facc15` | `#facc15` (동일) |
| `--color-brand-surface` | `#fef9c3` | `rgba(250,204,21,.16)` |
| `--color-brand-border` | `#facc15` | `rgba(250,204,21,.45)` |
| `--color-brand-ink` | `#854d0e` | `#fde047` |

**출처 구분 (정확히):**

| 구분 | 값 |
|---|---|
| **레포에 선례 없음** — Tailwind 팔레트에서 새로 가져옴 | `#fef08a` `#eab308` `#a7f3d0` `#fecaca` `#34d399` `#6ee7b7` `#fbbf24` `#fcd34d` `#f87171` `#fca5a5` `#047857`(성공 솔리드 대안) |
| **`ContractCheckClient.tsx:29-34`의 실재 페어** (AA 통과) | `#fee2e2`/`#b91c1c` · `#dcfce7`/`#15803d` · `#e2e8f0`/`#475569` · `#fef3c7`/`#b45309` |
| **레포 전역에 흩어져 있던 값의 승격** (위 파일 밖) | `#92400e`(warn-ink, 17곳 — `CalloutBox.tsx:5` 등. **`:29-34`에는 없다**) · `#059669` · `#dc2626` · `#166534` · `#fde68a` · `#facc15` 등 노랑 램프 전량 |
| **기존 blue 램프에서 역할만 새로 부여** (새 hex 없음) | `--color-accent-ink` `#1b64da`/`#4593fc` · `--color-accent-ink-hover` `#1957c2`/`#64a8ff` |

`--color-warn-ink`가 `#b45309`(같은 파일의 risk 전경)가 아니라 `#92400e`인 이유는 §6.4 말미 참조 — 전역 출현 빈도(17곳)와 대비(6.37 vs 4.51:1) 양쪽에서 후자가 낫다.

### A.2 신규 타이포 규정

| 항목 | 값 |
|---|---|
| hero | 700 / LH 1.15 / LS -0.02em |
| 2xl (h2) | 700 / LH 1.3 / LS -0.02em |
| xl (h3) | 600 / LH 1.4 / LS -0.015em |
| lg (h4) | 600 / LH 1.45 / LS -0.01em |
| sm | 400(라벨 500) / LH 1.6 / LS -0.01em |
| xs | 500 / LH 1.5 / LS 0 |
| 트래킹 상한 | -0.02em |
| 웨이트 래더 | 400 / 500 / 600 / 700만 |

### A.3 신규 레이아웃·모션 결정

| 항목 | 값 |
|---|---|
| 카드 패딩 | `p-5 sm:p-6` (20 / 24px) |
| 터치 타깃 하한 | 44 × 44px |
| 콘텐츠 최대폭 | 1400 / 1100 / 820px 3단 |
| 카드 반경 | `--radius-xl` 14px 단일 |
| 카드 보더 | 1px 단일 (`border-2` 폐기) |
| 카드 hover | `--shadow-md` + `translateY(-2px)` |
| `--shadow-xl` | 모달 전용으로 격하 |
| 버튼 hover | `scale-105` → `translateY(-1px)` |
| 표 셀 패딩 / LH | `px-4 py-3` / 1.5 |
| 표 헤더 배경 | `--color-bg-elevated` (라이트 `#ffffff` / 다크 `#1e2530`) + 하단 `1px solid var(--color-border)` |
| 표 반경 | 카드와 동일 `--radius-xl` 14px |
| 링크 | `--color-accent-ink` + 상시 밑줄(산문 내 인라인) |
| 44px 예외 | 조밀 제어는 시각 30px + 투명 히트 44px |
| 접근성 | `prefers-reduced-motion: reduce`에서 이동·스케일 정지 |
| `tabular-nums` 적용 범위 | 정렬되는 표의 숫자 열 + `HolidayPayCalculator.tsx` 결과값에만 |

### A.4 신규 마이크로카피 표준

§8.4의 상태 문구 5종(빈 상태 / 진행 / 오류 / 검색 없음 / 면책)과 §8.5 라벨 규칙 4항.
